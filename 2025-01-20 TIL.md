### 서브쿼리

- SQL문 안에 포함된 또 다른 SQL문
- 메인 쿼리에 값을 제공하거나 조건을 지정하는 역할
- 일반적으로 괄호로 감싸져 있으며, SELECT, INSERT, UPDATE, DELETE 문에서 사용 가능

연관 여부에 따른 분류: 

- 비연관 서브쿼리
    - 서브쿼리가 독립적으로 실행되어 결과를 반환
    - 메인쿼리와 서브쿼리 간 상호작용 없음
- 연관 서브쿼리
    - 서브쿼리가 메인쿼리의 각 행마다 실행
    - 메인 쿼리의 데이터를 서브쿼리에서 참조
    

위치에 따른 분류: 

- 스칼라 서브쿼리
    - 서브쿼리가 컬럼 값으로 반환되어 SELECT 절에서 사용
    - 주로 집계 값이나 계산 결과를 포함할 때 사용
- 인라인 뷰
    - FROM 절에서 서브쿼리 결과를 테이블처럼 사용
    - 별칭을 붙여 활용 가능
- 중첩 서브쿼리
    - WHERE절에서 조건을 설정하기 위해 서브쿼리 사용
    - 서브쿼리 결과에 따라 메인 쿼리의 필터링 결정
    - HAVING절에서 조건을 설정하기 위해 집계 함수와 함께 활용
    

반환값 유형에 따른 분류: 

- 단일 행 서브쿼리
    - 서브쿼리가 단일 행과 단일 열을 반환
    - 비교 연산자(=,<,> 등)와 함께 사용
- 다중행 서브쿼리
    - 서브쿼리가 여러 행을 반환
    - 다중 행 연산자(IN, ANY, ALL)과 함께 사용
- 다중열 서브쿼리

서브쿼리 사용 시 유의사항

- 성능 최적화
    - 비연관 서브쿼리는 독립적으로 실행되므로 더 빠름
    - 연관 서브쿼리는 메인 쿼리의 각 행마다 실행되므로 성능 저하 가능
- 적절한 반환값 확인
    - 서브쿼리가 반환하는 값의 유형(단일 값, 다중 값)에 따라 연산자 선택이 중요
- JOIN 대체 가능성

### 집합연산자

- SQL에서 두 개 이상의 SELECT 결과를 결합하여 하나의 결과 집합을 생성
- 데이터베이스에서 집합 이론을 기반으로 작동
- 서로 다른 쿼리의 결과를 비교하거나 결합할 때 유용
- 집합 연산자 종류
    - UNION: 두 SELECT 결과를 합치되 중복 제거, 결과는 정렬된 상태로 반환
    - UNION ALL: 두 SELECT 결과를 합치되 중복 포함, 결과는 정렬되지 않음
    - INTERSECT: 두 SELECT 결과의 교집합
    - MINUS: 첫 번째 SELECT 결과에서 두 번째 SELECT 결과를 뺀 차집합
- 집합연산자 사용 시 유의사항
    - SELECT 절에 나열된 컬럼 수와 데이터 타입이 동일해야 함
    - 결과 집합의 순서는 기본적으로 보장되지 않음
    - ORDER BY는 최종 결과에만 사용 가능
    - 대량의 데이터 처리 시 성능에 유의해야 함
    - ALIAS는 맨 처음 SELECT 절을 따름

### 윈도우 함수

- SQL에서 행(ROW)을 기준으로 특정 범위(Window)를 정의하여 작업하는 함수
- 집계 함수와 함께 데이터를 그룹핑하지 않고도 계산 가능
- 주요 용도:
    - 순위 계산
    - 이동 평균
    - 누적 합계
    - 그룹별 최대값/최소값

순위 함수

- RANK: 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뜀)
- DENSE_RANK: 순위를 계산하며 동일한 값은 같은 순위를 부여(순위 건너뛰지 않음)
- ROW_NUMBER: 순위를 고유하게 매김(중복 없음)
