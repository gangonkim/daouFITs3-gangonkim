<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="example.MyBook">

    <insert id="insertComment" parameterType="org.example.servletex.vo.CommentVO">
        <![CDATA[
        INSERT INTO Comments (
            boardno, commentno, writer, writedate, content
        ) VALUES (
            #{boardno}, COMMENTNO_SEQ.NEXTVAL, #{writer}, SYSDATE, #{content}
                 )
        ]]>
    </insert>

    <!-- 특정 게시글의 댓글 목록 조회 -->
    <select id="selectCommentsByPostId" resultType="org.example.servletex.vo.CommentVO">
        SELECT boardno, commentno, writer, content, writedate
        FROM comments
        WHERE boardno = #{boardno}
        ORDER BY commentno DESC
    </select>

    <delete id="deleteComment" parameterType="int">
        <![CDATA[
        DELETE FROM comments WHERE commentno=#{commentno}
        ]]>
    </delete>


</mapper>