1. 지역변수(local variable), 자동변수(auto)- 중괄호 내부, 함수의 매개변수에서 사용되는 변수이다.- 지역변수의 지역은 함수의 내부, 중괄호 내부를 말한다- 만든 곳을 벗어나면 사라진다.- 변수의 메모리 공간이 소멸 된다.- 지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기값이 저장된다
2. 전역변수(global)- 중괄호 외부에 선언되는 변수이다- 어느 지역에서든 참조할 수 있다.- 전처리기 밑에 선언하고 반드시 상수로 초기화 해야 한다.- 초기값을 지정하지 않으면 기본값이 0으로 자동 초기화 된다.- 모든 곳에 접근 가능하고 프로그램 종료 전에 메모리가 소멸되지 않는다- 잘못 이용하면 유지보수, 재사용등이 어려워 질 수 있다.- 이것의 단점을 보안한 것이 정적변수이다
3. 정적변수(static)- 프로그램 종료되기 전까지 메모리가 소멸되지 않는 변수이다- 함수를 벗어나도 변수가 사리지지 않고 유지된다 - 초기화할 때 반드시 상수로 초기화 해야 한다- 초기화하지 않으면 디폴트값 0으로 자동 초기화 된다
1) 정적변수의 종류는- 정적 전역변수(static global variable) : 자신이 선언된 소스 파일에서만 사용할 수 있고  
외부에서는 가져다 쓸 수 없다.
즉 전역변수에 static을 붙이면 변수의 범위를 파일 범위로 제한하는 효과를 낸다.- 정적 지역변수(static local variable) : 중괄호 내부에서만 사용할 수 있다. 한번 초기화 되
면 이후에 함수 호출시 값의 초기화를 무시한다

```
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void printGlobalVar();
void modifyGlobalVar();
// 특정 위치의 문자열을 다른 문자열로 변경하는 함수
void replace_substring(char *str, int start, const char *replacement, int len) {
    memcpy(str + start, replacement, len);
}

int findIdCount = 0;
int findPwdCount = 0;

void findIdCountFunc() {
    findIdCount++;
    printf("총 %d회째 ID 찾기 요청합니다", findIdCount);
}

void findPwCountFunc() {
    findPwdCount++;
    printf("총 %d회째 PW 찾기 요청합니다", findPwdCount);
}

int main(void) {

    //문자열에 대해서 대문자를 소문자로 소문자를 대문자로 변경하여 출력하세요
    char str[] = "12ABCdeFG";
    char *ptr = str;

    while (*ptr != '\0') {
        if (*ptr >= 65 && *ptr <= 90) {
            *ptr = toupper(*ptr);
        } else if (*ptr >= 97 && *ptr <= 122) {
            *ptr = tolower(*ptr);
        }
        printf("%c", *ptr);

        ptr++;
    }

    //문자열 역순으로 출력하기
    char str1[] = "swimming";
    printf("영문 순방향: %s\n", str1);
    printf("영문 역방향: ");
    for (int i = strlen(str1); i >= 0; i--) {
        printf("%c", str1[i]);
    }

    char str2[] = "가나다라마바사";
    printf("한글 순방향: ");
    printf("한글 역방향");
    for(int i = strlen(str2) - 2 ; i>=0 ; i-= 2 ) {
        printf("%c%c", str2[i], str2[i+1]);
    }

    //문자열 숫자 변환
    char* s1 = "47476";
    char* s2 = "98765432";
    char* s3 = "3456.7634";

    int num1 = atoi(s1);      // 문자열을 int로 변환
    long num2 = atol(s2);     // 문자열을 long으로 변환
    double num3 = atof(s3);   // 문자열을 double로 변환

    printf("atoi: %d\n", num1);
    printf("atol: %ld\n", num2);
    printf("atof: %.4f\n", num3);

    //숫자 문자로 변환
    int num = 3198;
    char buffer[4];

    _itoa(num, buffer, 10);
    printf("buffer: %s\n", buffer);

    //문자열 replace
    char s4[] = "abcdefg";
    char s5[] = "012";

    strncpy(s4, s5, strlen(s5) ); // s1의 첫 n개의 문자열을 s1에 복사한다.

    printf("strncpy: %s\n", s4);




    char str5[] = "abcdefg";
    char str6[] = "012";

    replace_substring(str5, 0, s5, strlen(s5));

    printf("replace_substring: %s\n", str5);

    printGlobalVar(); // globalVar = 10
    modifyGlobalVar(); // globalVar = 15
    printGlobalVar(); // globalVar = 15 (변경된 값 유지)




    int choice;
    while (1) {
        printf("1. id찾기 \t 2.pw찾기 \t 3. 종료하기 \n");
        printf("원하시는 서비스 번호를 입력하세요: ");
        scanf_s("%d", &choice);

        if ( choice == 1 ) {
            findIdCountFunc();
        } else if ( choice == 2 ) {
            findPwCountFunc();
        } else if ( choice == 3 ) {
            exit(0);
        }
    }

    int a = 10; // 정수형 변수
    int b= 11;
    int *p = &a; // a의 주소를 저장하는 포인터 변수
    printf("변수 a의 값: %d\n", a);
    printf("변수 a의 주소: %p\n", &a);
    printf("포인터 p가 가리키는 주소: %p\n", p);
    printf("포인터 p가 가리키는 값: %d\n", *p); // 포인터 역참조

    //실습과제 22
    double d = 100.0;
    double *dpoint = &d;
    printf("변수 d의 값: %f\n", d);
    printf("변수 d의 주소 값: %p\n", &d);
    printf("dpoint(포인터 변수)의 값 : %p\n", dpoint);
    printf("*dpoint의 값: %f\n", *dpoint);
    printf("변수 d의 크기: %d\n", sizeof(d));
    printf("변수 d의 주소의 크기: %d\n", sizeof(&d));
    printf("dpoint의 크기: %d\n", sizeof(dpoint));
    printf("*dpoint의 크기 %d\n", sizeof(*dpoint));



    //실습과제 23
    int i ,*ip = &i;
    int sum = 0, *sump = &sum;
    for (*ip = 1; *ip <= 100; (*ip)++) {
        *sump += *ip;
    }
    printf("sum = %d\n", sum);

    //실습과제 24
    int score = 100;
    int *pscore, **ppscore, ***pppscore;
    pscore = &score;
    ppscore = &pscore;
    pppscore = &ppscore;
    printf("포인터 변수 *pscore의 값은 %d\n", *pscore);
    printf("포인터 변수 *ppscore의 값은 %d\n", **ppscore);
    printf("포인터 변수 *pppscore의 값은 %d\n", ***pppscore);
    printf("============================\n");
    printf("int변수 score의 주소는 %p\n", &score);
    printf("포인터 변수 pscore의 값은 %p\n", pscore);
    printf("=============================\n");
    printf("포인터 변수 pscore의 주소는 %p\n", &pscore);
    printf("포인트 변수 ppscore의 값은 %p\n", ppscore);
    printf("=============================\n");
    printf("포인터 변수 ppscore의 주소는 %p\n", &ppscore);
    printf("포인터 변수 ppscore의 값은 %p\n", pppscore);

    //실습과제 25
    int score_[6] = {99, 89, 98, 88, 85};
    int *pscore_, i_ , sum_ = 0;
    pscore_ = &score_;
    if (pscore_ + 2 == score_ + 2 && pscore_ + 2 == &score_[2]) {
        printf("두 번째 요소의 주소가 모두 같다\n");
    }
    if (*(pscore_ + 2) == *(score_+2) && *(pscore_ + 2) == score_[2]) {
        printf("두 번째 요소의 값도 모두 같다\n");
    }
    printf("포인터 변수 pscore의 값: %p\n", pscore_);
    printf("배열 이름 score의 값: %p\n", score_);
    printf("포인터 변수 pscore의 값의 주소: %p\n", &pscore_);
    printf("배열 이름 score의 값의 주소: %p\n", &score_);
    sum = 0;
    for (i = 0; i < 5; i++) {
        sum = sum + *pscore++;
    }
    printf("포인터 변수를 이용한 합계는 %d\n", sum);




    
    return 0;


}

```
