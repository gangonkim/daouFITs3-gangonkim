인터넷: 물리적인 platform

이 망 위에서 서비스가 실행

데이터 통신을 위해 주소를 이용 → IP주소(논리적인 주소)

NIC(랜카드)에 할당

통신을 위해 물리적인 주소 필요 → MAC

ip주소를 문자로 사용 → DNS

IP주소 → 네트워크에 참여하고 있는 컴퓨터를 식별

 

## 2.4 **Port**

- IP 주소는 하나의 컴퓨터에 할당된 번호입니다.
- 이 컴퓨터에서 network를 사용하는 여러 개의 프로그램이 동시에 실행되고 있다고 가정하면 이 때, IP 주소 만을 가지고는 이 컴퓨터의 특정 network 프로그램에 접근할 수 없습니다.
- 이런 상황에서 해당 프로그램들을 구분할 수 있게 해 주는 것이 바로 port 번호입니다.

## 2.5 **HyperText**

- `HyperText`(하이퍼텍스트)는 사용자의 선택에 따라 관련된 정보로 이동할 수 있도록 조직화된 정보를 의미합니다. 이러한 관련 정보로의 이동을 도와주는 것이 바로 링크, 또는 하이퍼링크입니다.
- 하이퍼텍스트는 월드 와이드 웹(World Wide Web, WWW)의 발명을 이끈 주요 개념입니다.
- 웹은 수많은 하이퍼텍스트 링크로 거대한 양의 정보가 서로 연결된 것을 의미합니다.

## 2.6 **World Wide Web(WWW, W3)**

- 인터넷에 연결된 컴퓨터들을 통해 사람들이 정보를 공유할 수 있는 전 세계적인 정보 공간을 의미하며, 간단히 웹(Web)이라고 부릅니다.
- 이 용어는 인터넷과 동의어로 쓰이는 경우가 많으나, 엄격히 말해 서로 다른 개념입니다. 웹은 인터넷 상에서 동작하는 하나의 서비스입니다.
- 하이퍼텍스트는 웹 브라우저라 불리는 프로그램을 통해 웹 서버에서 문서나 웹 페이지 등의 정보를 읽어 들여 출력하는 형태로 보입니다. 사용자는 각 페이지의 하이퍼링크를 통해 다른 문서로 이동하거나, 해당 페이지를 서비스하는 서버로 정보를 보낼 수 있습니다.
- 하이퍼링크를 따라 이동하는 행위를 웹 서핑 혹은 웹 브라우징이라 합니다. 그리고 관련된 웹 페이지들의 집합을 웹 사이트라 합니다.

## 2.7 **HTTP**

- `HyperText Transfer Protocol`의 약자로 `Web`에서 정보를 주고받기 위해 사용하는 프로토콜입니다.
- 주로 HTML 문서를 주고받는 데 사용하며, `Client-Server` 구조를 이용하고 서버 프로세스의 기본 포트번호는 80번입니다.
- 클라이언트와 서버 사이에 이루어지는 요청/응답(`request`/`response`) 프로토콜입니다.
- HTTP 프로토콜은 클라이언트의 데이터 요청에 대한 응답 후 바로 연결을 해제합니다. 지속적인 연결이 아닌, 필요할 때마다 연결하고 데이터를 받은 후 즉시 연결을 끊는 방식입니다. 이런 프로토콜을 `stateless protocol`이라고 부르며, 장점은 많은 사용자가 동시에 서버 접속이 가능하다는 것이고, 단점은 각 사용자의 행위를 `tracking`하기 어렵다는 것입니다.

## 2.8 **Web Browser**

- 웹 브라우저는 웹 서버로부터 받은 HTML 문서나 파일을 연동하고 출력하는 응용 소프트웨어입니다.
- 개인용 컴퓨터에서 주로 사용하는 웹 브라우저에는 MS의 인터넷 익스플로러(Edge), 모질라 파이어폭스, 오페라사의 오페라, 애플의 사파리, 구글의 크롬 등이 있습니다.
- 웹 브라우저는 웹 서버로부터 웹 페이지를 가져오기 위해 HTTP 프로토콜을 이용하며, 웹 페이지를 가져올 뿐 아니라 웹 서버에 정보를 송신하기도 합니다.

## 2.9 **Web Server**

- 웹 브라우저(web client)로부터 HTTP 요청을 받아들이고, HTML 문서와 같은 웹 페이지를 전송하는 서비스 프로그램을 지칭합니다.
- 또한 위의 기능을 제공하는 컴퓨터 프로그램을 실행하는 컴퓨터(하드웨어)를 웹 서버라고 부르기도 합니다.

### **정적 웹 (Static Web)**

정적 웹은 사용자가 요청한 페이지가 변하지 않고 항상 동일한 콘텐츠를 제공하는 웹사이트입니다. HTML, CSS, JavaScript로 만들어지며, 서버는 사용자가 요청할 때 저장된 파일을 그대로 전달합니다.

### **특징**

- 데이터베이스를 사용하지 않음
- HTML, CSS, JavaScript 파일만으로 구성됨
- 사용자와의 상호작용이 제한적임
- 속도가 빠르고 보안이 뛰어남
- 유지보수가 어려움 (페이지가 많아질 경우 개별 수정 필요)

### **예시**

- 개인 포트폴리오 사이트
- 회사 소개 페이지
- 블로그 (Jekyll, Hugo 등 정적 사이트 생성기 사용)

---

### **동적 웹 (Dynamic Web)**

동적 웹은 사용자의 요청에 따라 실시간으로 콘텐츠가 변경되는 웹사이트입니다. 데이터베이스와 서버 사이드 언어(PHP, Python, Node.js 등)를 사용하여 페이지가 동적으로 생성됩니다.

### **특징**

- 데이터베이스를 사용하여 실시간으로 콘텐츠 변경 가능
- PHP, Python, Node.js 등 서버 사이드 언어 사용
- 사용자와의 상호작용이 많음 (로그인, 게시판, 댓글 등)
- 유지보수가 상대적으로 쉬움 (데이터를 수정하면 전체 사이트 반영 가능)
- 속도가 느릴 수 있으며 보안 취약점이 존재할 가능성이 있음

### **예시**

- SNS (Facebook, Instagram, Twitter)
- 온라인 쇼핑몰 (쿠팡, G마켓)
- 포털 사이트 (네이버, 구글)

---

### **정적 웹 vs 동적 웹 비교**

| 비교 항목 | 정적 웹 | 동적 웹 |
| --- | --- | --- |
| 콘텐츠 변경 | 고정됨 | 사용자 요청에 따라 변경됨 |
| 데이터베이스 | 사용 안 함 | 사용함 |
| 속도 | 빠름 | 상대적으로 느림 |
| 보안 | 상대적으로 안전 | 보안 위험 존재 |
| 유지보수 | 어려움 | 비교적 쉬움 |
| 예시 | 블로그, 포트폴리오 | SNS, 쇼핑몰, 커뮤니티 |

**📌 결론:**

- 간단한 사이트(랜딩 페이지, 포트폴리오)는 **정적 웹**이 적합
- 사용자와의 상호작용이 필요한 사이트(커뮤니티, 쇼핑몰)는 **동적 웹**이 적합7

## **GET 방식**

- 브라우저의 주소창에 직접 입력방식으로 실습(Query String 이용)
- `http://127.0.0.1:8080/lab/login?loginId=aaa&pwd=bbb`
- 링크(a 태그 이용)를 이용한 인자 전달방식 실습
- `<a href = "http://127.0.0.1:8080/lab/login?loginId=aaa&pwd=bbb">이동</a>`

## **POST 방식**

- form을 이용한 인자 전달방식 실습
- textfield의 값을 받는 경우
- checkbox안에 check된 항목의 값을 받는 경우

servlet을 실행하려면

1. protocol ⇒ http
2. 서버 쪽 컴퓨터의 ip ⇒ localhost
3. 웹서버의 포트 번호 ⇒ 8080
4. 프로젝트의 context ⇒ HelloServletProject_war_exploded
5. 해당 프로젝트의 servlet에 대한 url mapping ⇒ /myservlet

→ http://localhost:8080/HelloServletProject_war_exploded/myservlet
