Step1 → 일반적인 DAO (중복코드존재)

Step2 → 중복된 코드를 method로 분리

Step3 → 추상 메서드와 추상 클래스로 만들어서 제공

### **Dependency Injection (DI, 의존성 주입)란?**

- *Dependency Injection(DI)**는 객체 간의 **의존 관계를 외부에서 주입**해 주는 디자인 패턴입니다. 이를 통해 **객체의 결합도를 낮추고, 유연성과 테스트 용이성을 향상**시킬 수 있습니다.

---

## **1. 왜 Dependency Injection이 필요한가?**

일반적으로 객체는 다른 객체를 직접 생성하여 의존하는 경우가 많습니다. 하지만 이렇게 하면 코드가 **강하게 결합(Tightly Coupled)** 되어 변경이 어렵고, 테스트하기도 어려워집니다.

### **예제: DI가 없는 코드 (문제점)**

```java
java
복사편집
class Service {
    void doSomething() {
        System.out.println("Service is doing something.");
    }
}

class Client {
    private Service service;

    Client() {
        this.service = new Service();  // 직접 객체 생성 (강한 결합)
    }

    void execute() {
        service.doSomething();
    }
}

```

### **문제점**

- `Client` 클래스가 `Service` 객체를 직접 생성하기 때문에, `Service`의 변경이 `Client`에 영향을 줌.
- 단위 테스트에서 `Service`를 Mocking하기 어려움.

---

## **2. Dependency Injection 적용**

DI를 적용하면 **객체를 직접 생성하는 대신 외부에서 주입**받도록 변경할 수 있습니다.

### **예제: 생성자 주입을 활용한 DI**

```java
java
복사편집
class Service {
    void doSomething() {
        System.out.println("Service is doing something.");
    }
}

// Client는 Service를 직접 생성하지 않고 외부에서 주입받음
class Client {
    private Service service;

    // 생성자를 통한 의존성 주입
    Client(Service service) {
        this.service = service;
    }

    void execute() {
        service.doSomething();
    }
}

```

### **개선점**

- `Client` 클래스가 `Service`의 구체적인 구현을 몰라도 됨 (의존성이 낮아짐)
- `Service`를 쉽게 교체할 수 있음 (Mock 객체 활용 가능)

---

## **3. Dependency Injection의 유형**

1. **생성자 주입 (Constructor Injection)**
    - 객체의 의존성을 생성자를 통해 주입
    - 필수적인 의존성을 보장할 수 있음
    - 가장 권장되는 방식
    
    ```java
    java
    복사편집
    class Client {
        private final Service service;
    
        Client(Service service) {
            this.service = service;
        }
    }
    
    ```
    
2. **세터 주입 (Setter Injection)**
    - `setter` 메서드를 통해 의존성을 주입
    - 선택적인 의존성 주입이 가능하지만, 객체가 완전히 초기화되지 않은 상태에서 사용될 위험이 있음
    
    ```java
    java
    복사편집
    class Client {
        private Service service;
    
        void setService(Service service) {
            this.service = service;
        }
    }
    
    ```
    
3. **인터페이스 주입 (Interface Injection)**
    - 인터페이스를 통해 의존성을 주입
    - 사용이 복잡하여 잘 쓰이지 않음

---

## **4. Spring 프레임워크에서의 DI**

Spring에서는 `@Autowired` 어노테이션을 사용하여 자동으로 의존성을 주입할 수 있습니다.

```java
java
복사편집
@Component
class Service {
    void doSomething() {
        System.out.println("Service is doing something.");
    }
}

@Component
class Client {
    private final Service service;

    @Autowired  // 생성자 주입
    Client(Service service) {
        this.service = service;
    }

    void execute() {
        service.doSomething();
    }
}

```

Spring은 내부적으로 **IoC (Inversion of Control)** 컨테이너를 사용하여 객체의 생명주기를 관리하고, 필요한 곳에 객체를 주입해 줍니다.

---

## **5. Dependency Injection의 장점**

✅ **유지보수 용이**: 객체 간 결합도가 낮아져 코드 변경이 용이

✅ **테스트 용이**: Mock 객체를 주입하여 단위 테스트 수행 가능

✅ **유연성 증가**: 필요에 따라 객체를 쉽게 교체 가능

✅ **코드 재사용성 증가**: 객체를 재사용하기 쉬워짐

---

## **6. 결론**

Dependency Injection은 객체 간의 의존성을 관리하여 코드의 결합도를 낮추고 유지보수성을 높이는 중요한 설계 패턴입니다. Spring과 같은 프레임워크에서는 DI를 기본적으로 지원하여 개발자의 부담을 줄여 줍니다.
