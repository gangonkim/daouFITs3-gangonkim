# 11. **상속과 관련된 constructor의 특징**

- constructor는 상속되지 않는다.
- 프로그래머가 명시하지 않아도 constructor가 호출될 때 자동적으로 상위 class의 constructor를 호출한다.
- sub class의 객체를 생성할 경우 먼저 super class의 객체가 만들어 져야 하기 때문에 super class의 constructor가 호출
- 주의할 것은 이때 자동적으로 호출되는 상위 class의 constructor는 인자가 없는 constructor가 호출된다는 것이다.

![image.png](attachment:dcab5f4a-e949-4bd3-b7a2-d551ffc2b69a:image.png)

![image.png](attachment:bea87866-9a0b-4c1f-9418-776fc80df115:image.png)

# 12. **method overriding**

- overriding은 부모로부터 물려받은 method의 내용을 하위 class에서 다시 정의하는 것.
- method overriding의 조건
- method의 이름이 같아야 한다.
- method의 return type이 같아야 한다.
- method의 parameter개수와 type이 같아야 한다.
- access modifier는 부모의 것과 같거나 더 넓어야 한다.

# 13. **is-a Relationship**

- `Sub class is a Super class`
- sub class type을 써야 하는 자리에 super class의 type을 사용해도 된다.
- Type casting

```java
package inheritance;

public class SuperClass {

    static int staticCall(String msg) {
        System.out.println(msg);
        return 0;
    }

    int a = staticCall("SuperClass의 지역변수");
    static int b = staticCall("SuperClass의 static변수");

    public SuperClass() {
        staticCall("SuperClass의 constructor");
    }

    public SuperClass(int a) {
        this();
        staticCall("overloading된 constructor");
    }

    public void myFunction() {
        System.out.println("SuperClass의 myFunction");
    }
}
```

```java
package inheritance;

public class InheritanceTest extends SuperClass{

    int c = staticCall("InheritanceTest의 지역변수");
    static int d = staticCall("InheritanceTest의 static변수");

    public InheritanceTest() {
        super(100);
        staticCall("InheritanceTest의 생성자");
        super.myFunction();
    }

    @Override
    public void myFunction() {
        System.out.println("Override된 myFunction");
    }

    public static void main(String[] args) {
        System.out.println("main 실행");
        SuperClass s = new InheritanceTest();
        s.myFunction();
    }
}
```

실행순서

1. SuperClass의 static변수
2. InheritanceTest의 static변수
3. main 실행
4. SuperClass의 지역변수
5. SuperClass의 constructor
6. overloading된 constructor
7. InheritanceTest의 지역변수
8. InheritanceTest의 생성자
9. SuperClass의 myFunction
10. Override된 myFunction

# 15. **Abstraction**

- abstraction은 객체들의 공통적인 특징(속성과 기능)을 뽑아내서 이름을 붙이는 것.
- C의 구조체 역시 추상화 ( 자료의 추상화 )
- C의 function, Java의 method 역시 추상화 ( 기능의 추상화 )
- class를 정의하는 과정 그 자체가 추상화.

# 16. **Encapsulation**

- 데이터 구조와 데이터를 다루는 방법을 결합시켜 묶은 것을 지칭.
- class로 부터 파생된 객체가 독립적으로 역할을 수행하기 위해 데이터(field)와 기능(method)을 묶어서 관리
- Encapsulation을 하는 이유: class의 재활용성을 높이고 에러발생을 최소화

# 17. **Information Hiding**

- class는 외부에서 알 필요가 없는 field나 method를 외부에 노출 시킬 필요가 없다.
- 불필요한 정보는 숨기고 최소한의 정보만을 이용하여 class를 쉽게 사용할 수 있어야 한다.
- Java에서는 access modifier를 이용하여 선언된 class의 외부에서의 접근을 제어하면서 정보를 은폐하는 방식을 이용한다.
- Encapsulation된 코드를 제공할 때 외부에서 함부로 수정, 호출해서는 안 되는 field, method를 보호하고 프로그램의 안정적인 수행을 위해 필요.

# 18. **final keyword**

- class 앞에 final이 붙었을 경우 : 해당 class를 상속할 수 없음을 의미
- field 앞에 final이 붙었을 경우 : 해당 field의 값을 변경할 수 없음을 의미 ( 상수화 )
- method 앞에 final이 붙었을 경우 : 해당 method를 하위 class에서 overriding할 수 없음을 의미

# 19. **abstract class**

- abstract class의 특징 : abstract class는 instance를 생성할 수 없다
- abstract class를 상속받은 하위 class에서 abstract method를 overriding 한 후 instance 생성
- 추상 메서드가 하나라도 존재하면 추상 클래스로 만들어야함

# 20. **polymorphism & dynamic binding**

다형성은 부모의 객체 타입으로 다양한 형태의 자식 객체를 생성,사용할 수 있는 것을 의미.

![image.png](attachment:c4612980-2b75-46c9-9716-646c7c902556:image.png)

다형성을 이용하여 상위 데이터 타입으로 객체의 타입을 통일해서 사용

![image.png](attachment:0f0dea8e-7ec8-4488-9224-f335ce8d16f0:image.png)

다형성을 이용해 객체들을 상위 데이터 타입으로 통일하면 프로그램의 처리가 쉬워짐.

![image.png](attachment:de53dc49-17ca-4544-8d78-f0a497d47f31:image.png)

다음은 동적 바인딩에 대한 내용입니다.

![image.png](attachment:5be3b95e-6c3d-4db8-8928-54388d0f6d04:image.png)

![image.png](attachment:9812c9a8-3aac-4fa3-b21b-8eb8b92dd92d:image.png)

다음과 같은 경우 에러가 발생하기 때문에 조심해야 합니다.
