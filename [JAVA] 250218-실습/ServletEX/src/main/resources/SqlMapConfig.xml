<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org/DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- JDBC 드라이버 클래스와 계정 설정-->
    <!--이 연결 설정에 대한 내용은 xml안에 직접 작성하지 않아야 함-->

    <properties resource="./driver.properties"/>

    <!--mybatis 실행 설정-->
<!--    대부분 default설정 사용-->
    <settings>
        <setting name='jdbcTypeForNull' value='NULL'/>
    </settings>
<!--Type alias가 나올 수 있음-->
    <typeAliases>
        <typeAlias alias="Book" type="org.example.servletex.vo.BookVO"/>
    </typeAliases>
    
    <environments default='development'>
        <environment id="development">
            <transactionManager type="JDBC"/>
            <!--커넥션풀 사용 여부 결정-->
            <dataSource type="POOLED">
                <property name="driver" value='${db.driver}'/>
                <property name='url' value='${db.url}'/>
                <property name='username' value='${db.username}'/>
                <property name='password' value='${db.password}'/>
            </dataSource>
        </environment>
    </environments>

    <!--SQL구문을 담은 xml파일위치-->
    <mappers>
        <mapper resource='./sqlmap/Book.xml'></mapper>
        <mapper resource='./sqlmap/Member.xml'></mapper>
        <mapper resource='./sqlmap/Board.xml'></mapper>
    </mappers>


</configuration>