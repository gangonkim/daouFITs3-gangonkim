# 2. **String class**

• String instance는 immutable.



```java
class MyTest {

	public static void main(String[] args) {

		String str1 = "Hello";
		String str2 = "Hello";
		String str3 = new String("Hello");
		
		System.out.println(str1 == str2);  // true
		System.out.println(str1 == str3);  // false
		
		System.out.println(str1.equals(str3));  // true
		
	}
}
```

---

# 3. **Array**



```java
class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		int arr1[] = new int[3];
		Student arr2[] = new Student[3];
	}

}
```

배열은 사이즈 고정, ArrayList는 크기가 유동적

ArrayList는 다른 Datatype를 저장할 수 있음

ArrayList는 primitive타입 저장 불가, 인스턴스만 저장할 수 있음.

근데 autoboxing으로 primitive을 wrapperclass로 변환해서 저장해줌

제네릭에 신경써서 할것

**List와 ArrayList의 차이

List는 인터페이스, ArrayList는 클래스

# 4. **ArrayList**

```java
ArrayList list = new ArrayList();
```

```java
import java.util.ArrayList;
import java.util.List;

class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		List list = new ArrayList<>();
		list.add("Hello");
		list.add(100);
		list.add(new Student());
		
		for(Object obj : list) {
			System.out.println(obj);
		}
		
		List<String> genericList = new ArrayList<String>();
		genericList.add("홍길동");
		genericList.add("최길동");
//		genericList.add(100);  // error
		System.out.println(genericList.get(0));  // 홍길동
	}

}
```

# 5. **HashMap class**

• ( key , value ) 쌍으로 이루어진 data를 저장할 수 있는 자료구조



```java
import java.util.HashMap;
import java.util.Map;

class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		Map<String,String> map = new HashMap<String,String>();
		map.put("1", "홍길동");
		map.put("2", "최길동");
		
		System.out.println(map.get("2")); // 최길동
		
		for (String key : map.keySet() ) {
			System.out.println(key + " - " + map.get(key));        
		}
	}

}
```

---

# 6. **HashSet**

```java
HashSet set = new HashSet();
```

```java
import java.util.HashSet;
import java.util.Iterator;
import java.util.Set;

class Student {
	
}

class MyTest {

	public static void main(String[] args) {

		Set<String> set = new HashSet<String>();
		set.add("홍길동");
		set.add("아이유");
		set.add("홍길동");
		
		Iterator<String> iter =  set.iterator();
		while(iter.hasNext()) {
			System.out.println(iter.next());
		}
	}

}
```

---

# 7. **Exception**


- Exception에 관련된 세부 정보를 가지고 있는 객체 생성
- Exception객체가 처리되지 않으면 program은 비정상 종료.
- Java에서 Exception을 처리하는 유일한 방법은 try ~ catch 구문
