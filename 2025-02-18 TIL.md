HTTP 세션(HTTP Session): 클라이언트(사용자)와 서버 간의 상태를 유지하는 방법. 

HTTP 자체는 **stateless(상태를 저장하지 않는)** 프로토콜이므로, 기본적으로 요청(Request)과 응답(Response)이 끝나면 서버는 클라이언트의 상태를 기억하지 않음

이를 해결하기 위해 세션(Session)이 사용

---

## 🔹 HTTP 세션의 개념

HTTP 세션은 **클라이언트가 서버에 접속한 후 종료할 때까지의 상태를 유지하는 기술**입니다. 이를 통해 사용자의 로그인 정보나 장바구니 데이터 같은 상태 정보를 유지할 수 있습니다.

- **세션 생성**: 사용자가 서버에 처음 요청하면, 서버는 고유한 `Session ID`를 생성하고 클라이언트에게 전달합니다.
- **세션 유지**: 이후 요청에서 클라이언트는 `Session ID`를 함께 전송하여 서버가 사용자를 식별할 수 있도록 합니다.
- **세션 종료**: 일정 시간이 지나거나 사용자가 로그아웃하면 세션이 종료됩니다.

---

## 🔹 HTTP 세션의 동작 방식

1. **클라이언트가 서버에 요청을 보냄.**
2. **서버가 세션을 생성하고, `Session ID`를 클라이언트에게 응답으로 전달.**
3. **클라이언트가 이후 요청 시 `Session ID`를 함께 전송.**
4. **서버는 `Session ID`를 확인하여 해당 사용자의 상태 정보를 유지.**
5. **세션 만료 시(`timeout` 또는 로그아웃) 데이터 삭제.**

---

## 🔹 HTTP 세션 vs 쿠키(Cookie)

|  | **세션(Session)** | **쿠키(Cookie)** |
| --- | --- | --- |
| **저장 위치** | 서버 | 클라이언트(브라우저) |
| **보안성** | 상대적으로 높음 (서버 저장) | 상대적으로 낮음 (클라이언트 저장) |
| **유효기간** | 설정 가능 (보통 일정 시간 후 자동 만료) | 브라우저 종료 또는 만료 시간까지 유지 |
| **데이터 크기** | 비교적 큼 (서버 저장) | 작음 (4KB 제한) |
| **사용 목적** | 로그인 정보, 장바구니 등 | 사용자 설정 정보(테마, 자동 로그인 등) |

※ 쿠키는 브라우저가 직접 저장하지만, 세션은 서버에서 저장되며, 클라이언트는 `Session ID`만 보관합니다.

---

## 🔹 HTTP 세션을 활용하는 예시

1. **사용자 로그인 유지**
    - 사용자가 로그인하면 서버가 `Session ID`를 발급하고, 이후 요청에서 이를 사용하여 로그인 상태를 유지합니다.
2. **장바구니 기능**
    - 사용자가 상품을 추가하면 세션에 정보를 저장하고, 페이지를 새로고침해도 유지됩니다.
3. **보안이 중요한 기능**
    - 세션을 활용하면 중요한 데이터를 서버에서 관리할 수 있어, 쿠키보다 보안성이 높습니다.

---

## 🔹 세션 관리 방식

### 1. **세션 저장소**

- **메모리 저장**: 빠르지만, 서버가 재시작되면 데이터가 사라짐.
- **데이터베이스(DB) 저장**: 영속성이 있지만 성능 저하 가능성 있음.
- **Redis 같은 캐시 서버 활용**: 빠르고 안정적이나 추가적인 설정이 필요.

### 2. **세션 유지 시간**

- 일정 시간이 지나면 자동으로 만료 (`timeout` 설정 가능).
- 로그인 시 `Remember Me` 기능 등을 통해 세션 연장 가능.

### 3. **세션 보안**

- **HTTPS 사용**: 세션을 탈취하는 `세션 하이재킹(Session Hijacking)` 방지.
- **SameSite 쿠키 설정**: 크로스 사이트 요청 위조(CSRF) 공격 방지.
- **Session Fixation 방지**: 로그인 후 새로운 세션 ID 발급.

---

## 🔹 정리

- HTTP 세션은 **클라이언트의 상태를 유지하는 기술**로, `Session ID`를 통해 서버가 사용자를 식별.
- 세션은 **서버에서 관리**, 쿠키는 **클라이언트(브라우저)에서 관리**.
- 로그인 유지, 장바구니 등 다양한 기능에 사용됨.
- 보안을 위해 **HTTPS, 세션 만료 시간 설정, 보안 쿠키** 등을 활용하는 것이 중요.

## 예시 코드(세션)

### **1️⃣ 로그인 시 세션 저장**

사용자가 로그인하면 `Session ID`를 생성하고, 사용자 정보를 세션에 저장하는 코드입니다.

### 📌 `LoginServlet.java`

```java
java
복사편집
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

@WebServlet("/login")
public class LoginServlet extends HttpServlet {
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        String username = request.getParameter("username");
        String password = request.getParameter("password");

        // 간단한 인증 로직 (실제로는 DB에서 확인해야 함)
        if ("admin".equals(username) && "1234".equals(password)) {
            HttpSession session = request.getSession();  // 세션 생성 또는 가져오기
            session.setAttribute("user", username);  // 세션에 사용자 정보 저장
            session.setMaxInactiveInterval(30 * 60);  // 세션 유지 시간 (30분)

            response.sendRedirect("welcome.jsp");  // 로그인 성공 후 이동
        } else {
            response.sendRedirect("login.jsp?error=1");  // 로그인 실패 시
        }
    }
}

```

---

### **2️⃣ 로그인 후 세션 확인**

로그인 후 `welcome.jsp`에서 세션 정보를 가져와 사용자 이름을 표시합니다.

### 📌 `welcome.jsp`

```jsx
jsp
복사편집
<%@ page import="javax.servlet.http.HttpSession" %>
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<html>
<head>
    <title>Welcome</title>
</head>
<body>
    <%
        HttpSession sessionObj = request.getSession(false); // 세션 가져오기 (없으면 null)
        String user = (sessionObj != null) ? (String) sessionObj.getAttribute("user") : null;

        if (user != null) {
    %>
        <h2>안녕하세요, <%= user %>님!</h2>
        <a href="logout">로그아웃</a>
    <%
        } else {
            response.sendRedirect("login.jsp");  // 세션이 없으면 로그인 페이지로 리다이렉트
        }
    %>
</body>
</html>

```

---

### **3️⃣ 로그아웃 시 세션 삭제**

사용자가 로그아웃하면 세션을 무효화합니다.

### 📌 `LogoutServlet.java`

```java
java
복사편집
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;
import javax.servlet.http.HttpSession;

@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        HttpSession session = request.getSession(false); // 기존 세션 가져오기
        if (session != null) {
            session.invalidate();  // 세션 무효화
        }
        response.sendRedirect("login.jsp");  // 로그인 페이지로 이동
    }
}

```

---

## **📝 코드 설명**

1. 사용자가 로그인하면 **세션 생성 (`request.getSession()`)** 후 사용자 정보를 저장.
2. 로그인 후 `welcome.jsp`에서 **세션 정보를 가져와(`getAttribute()`)** 사용자 이름 표시.
3. 사용자가 로그아웃하면 **세션을 무효화(`invalidate()`)**하여 정보 삭제.

## 예시 코드(쿠키)

## **1️⃣ 로그인 시 쿠키 저장**

사용자가 로그인하면 쿠키를 생성하고, 이를 클라이언트에 저장합니다.

### 📌 `LoginServlet.java`

```java
java
복사편집
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/login")
public class LoginServlet extends HttpServlet {
    protected void doPost(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        String username = request.getParameter("username");
        String password = request.getParameter("password");

        // 간단한 인증 (실제로는 DB 검증 필요)
        if ("admin".equals(username) && "1234".equals(password)) {
            Cookie userCookie = new Cookie("username", username); // 쿠키 생성
            userCookie.setMaxAge(30 * 60); // 30분 동안 유지
            response.addCookie(userCookie); // 클라이언트에 쿠키 저장

            response.sendRedirect("welcome.jsp"); // 로그인 성공 시 이동
        } else {
            response.sendRedirect("login.jsp?error=1"); // 로그인 실패 시
        }
    }
}

```

---

## **2️⃣ 로그인 후 쿠키 확인**

로그인 후 `welcome.jsp`에서 쿠키 정보를 가져와 사용자 이름을 표시합니다.

### 📌 `welcome.jsp`

```
jsp
복사편집
<%@ page contentType="text/html;charset=UTF-8" language="java" %>
<html>
<head>
    <title>Welcome</title>
</head>
<body>
    <%
        String user = null;
        Cookie[] cookies = request.getCookies(); // 쿠키 가져오기
        if (cookies != null) {
            for (Cookie cookie : cookies) {
                if ("username".equals(cookie.getName())) {
                    user = cookie.getValue();
                    break;
                }
            }
        }

        if (user != null) {
    %>
        <h2>안녕하세요, <%= user %>님!</h2>
        <a href="logout">로그아웃</a>
    <%
        } else {
            response.sendRedirect("login.jsp"); // 쿠키가 없으면 로그인 페이지로 이동
        }
    %>
</body>
</html>

```

---

## **3️⃣ 로그아웃 시 쿠키 삭제**

사용자가 로그아웃하면 쿠키의 유효기간을 `0`으로 설정하여 삭제합니다.

### 📌 `LogoutServlet.java`

```java
java
복사편집
import java.io.IOException;
import javax.servlet.ServletException;
import javax.servlet.annotation.WebServlet;
import javax.servlet.http.Cookie;
import javax.servlet.http.HttpServlet;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpServletResponse;

@WebServlet("/logout")
public class LogoutServlet extends HttpServlet {
    protected void doGet(HttpServletRequest request, HttpServletResponse response)
            throws ServletException, IOException {

        Cookie userCookie = new Cookie("username", "");
        userCookie.setMaxAge(0); // 쿠키 즉시 삭제
        response.addCookie(userCookie);

        response.sendRedirect("login.jsp"); // 로그인 페이지로 이동
    }
}

```

---

## **📝 코드 설명**

1. 사용자가 로그인하면 **쿠키(`Cookie`)를 생성**하여 클라이언트에 저장.
2. 로그인 후 `welcome.jsp`에서 **쿠키 정보를 가져와(`getCookies()`)** 사용자 이름 표시.
3. 로그아웃 시 **쿠키를 삭제(`setMaxAge(0)`)**하여 로그인 상태 해제.
