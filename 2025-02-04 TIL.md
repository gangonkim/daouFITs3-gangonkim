### JAVA 언어 특징

- 같은 코드를 여러 플랫폼에서 사용 가능(운영체제에 독립적)
- 하이레벨 랭귀지로 작성한 코드를 컴파일러를 통해서 머신코드로 변경
- →자바는 약간 다르게 컴파일러를 통해서 바이트코드로 변경
- 이렇게 변경된 바이트 코드를 JVM이 읽어들여서 머신코드로 변경해서 실행
- .java → javac(java compiler) → .class → JVM

### JAVA는 객체지향언어

-절차적 언어: 대표적으로 C언어, 기능으로 프로그램을 세분화시킴 → 더이상 분할할 수 없는 단위기능이 나오게 됨 → 함수로 만듬

장점: 요구사항 분석이 쉬움, 프로그램 설계가 쉬움 → 비용절감 이점

단점: 확장, 유지보수가 힘듬

이 문제를 해결하기 위한 프로그래밍 패러다임이 변화 - > 객체지향 프로그래밍의 등장

해결해야하는 현실 세계를 프로그램으로 모델링하는 작업을 한 후에서 프로그래밍 진행

기능이 아니라 구성요소를 파악한 후 모델링

사람에 따라 설계가 달라짐

장점: 유지보수가 쉬움

단점: 설계가 힘듬

### JVM, JRE, JDK

JVM: 바이트코드를 읽어들여서 실행하는 실행 주체

JRE(JAVA RUMTIME Environment): 자바실행환경

→ JVM + Java Class Library

JDK(Java Development Kit): 자바개발도구

→ JRE + 자바개발도구(javac, java, javadoc, …)

### Java의 객체지향

현실세계의 구현해야 하는 문제를 프로그래밍적으로 표현(모델링)을 하는 구현 방법론

해결해야하는 문제를 구성하고 있는 구성요소를 파악

이 구성 요소를 파악한 후 프로그램으로 표현해야 함

추상화를 거쳐서 나오는 여러가지 정보를 

상태(잔액, 나이, 키 주소, …)와

행위(이체한다, 대출을 받는다, …)로 구분

상태는 변수(필드) 로, 행위는 함수(메서드)로 표현

이 필드와 메서드는 클래스라는 단위로 묶여서 표현

클래스라는 단위는 객체모델링을 하기 위한 수단으로 사용 → 클래스의 첫번째 의미

결국 자바 프로그램은 class의 집합으로 표현

코드로 표현할 때 class하나당 파일 한개

class이름을 파일이름으로

클래스 예시

class Customer {

String name;

int age;

메서드

}

이 클래스로부터 저장공간을 생성해서 그 저장공간을 이용해서 프로그래밍을 진행 

인스턴스 파생시키는 수단 → 클래스의 세 번째 의미

클래스는 추상데이터타입(ADT) → 클래스의 두 번째 의미

하나의 프로그램내에는 상당히 많은 class들이 존재

이런 클래스를 효율적으로 관리하기 위한 논리적인 개념 → package

package는 물리적으로 폴더로 표현

자바는 Strong Type 언어 

자바는 변수명 대소문자 구분, 길이의 제한이 없음

예약어로는 변수를 만들 수 없음

숫자로 시작하는 변수는 만들 수 없음

특수문자는 _와 $만 허용

클래스 이름은 pascal case

변수 이름은 camel case

자바의 데이터타입은 2가지

1. primitive date type(기본형): 8개
    
    해당 타입으로 선언된 변수 안에 직접 데이터가 저장
    
    정수형: byte(1바이트) , short(2바이트) , **int(4바이트)**, long(8바이트)
    
    실수형: float, **double**
    
    문자형: char
    
    논리형: boolean
    
2. reference date type(참조형): class
    
    클래스로 선언된 변수를 지칭
    
    이 타입으로 선언된 변수 안에는 메모리 주소가(실제로는 해쉬값) 저장
    

상수: final

ex) final int MY_AGE = 20

형변환:

묵시적 형변환

명시적 형변환(캐스팅) 

Overloading

같은 이름이나 같은 형태로 여러개의 기능을 수행하는 경우
