Pure JDBC

- sql과 java가 하나의 파일에 공존
- →별도의 라이브러리나 프레임워크를 가져다 씀

# 1. **MyBatis**

`MyBatis`를 한마디로 정의하자면 Database 연동을 도와주는 Framework이라고 할 수 있습니다. 

Controller에서 Service를 호출하고 Service가 DAO를 이용하여 Database처리를 할 때 DAO에서 이 MyBatis를 이용하여 Database처리를 하게 됩니다.

<aside>
✅ 기존의 JDBC는 연동 과정도 복잡하고 SQL문장이 코드내에 섞여 있어서 유지보수 하기가 상당히 불편했는데 이런점을 개선하여 편의성과 유지보수성을 높이기 위한 목적으로 만들어졌다고 생각하시면 됩니다.

</aside>

간단하게 특징을 살펴보면 다음과 같습니다.

- JDBC의 모든 기능을 MyBatis가 대부분 제공해줍니다. 따라서 `복잡한 JDBC 코드를 걷어낼 수 있으며 깔끔한 소스코드를 유지`할 수 있습니다.
- `SQL 실행결과를 Map객체에 mapping`해 줍니다.
- `SQL 문장을 소스코드가 아닌 XML로 분리하여 작성`합니다. 따라서 SQL의 변경이 있을 때마다 source code를 수정하거나 컴파일하지 않아도 됩니다.
- `DataSource기능과 Transaction처리기능을 제공`해줍니다.

```
(1) 커넥션 풀이란
JDBC API를 사용하여 데이터베이스와 연결하기 위해 Connection 객체를 생성하는 작업은 비용이 매우 많이 드는 작업입니다.

Connection 객체 생성 과정:
① 애플리케이션에서 DB 드라이버를 통해 커넥션을 조회합니다.
② DB 드라이버는 DB와 TCP/IP 커넥션을 연결합니다. (3 way handshake와 같은 네트워크 연결 동작 발생)
③ DB 드라이버는 TCP/IP 커넥션이 연결되면 아이디, 패스워드, 기타 부가 정보를 DB에 전달합니다.
④ DB는 아이디, 패스워드로 내부 인증을 거친 후 내부에 DB를 생성합니다.
⑤ DB는 커넥션 생성 완료 응답을 보냅니다.
⑥ DB 드라이버는 커넥션 객체를 생성해서 클라이언트에 반환합니다.

DB 연결마다 Connection 객체를 새로 만드는 것은 비용이 많이 들고 비효율적입니다. 이 문제를 해결하기 위해 커넥션 풀(Connection Pool)이 등장했습니다. 커넥션 풀은 애플리케이션 시작 시점에 Connection 객체를 미리 생성하고, 필요할 때 이를 재사용하여 성능을 향상시킵니다.

(2) 커넥션 풀 동작 구조
① 애플리케이션 시작 시 커넥션 풀이 필요한 만큼의 커넥션을 미리 생성하여 보관합니다.
② 서비스 특성에 따라 Connection 객체 수가 다르지만, 보통 기본값으로 10개를 생성합니다.
③ 풀의 Connection 객체는 이미 DB와 TCP/IP로 연결되어 있어 즉시 SQL을 전달할 수 있습니다.
④ 새로운 커넥션을 생성하는 대신 이미 있는 커넥션을 재사용합니다.
⑤ 커넥션 요청 시 풀은 보유 중인 커넥션 객체 중 하나를 반환합니다.

이를 통해 커넥션 조회, 연결, 인증 등 객체 생성 과정을 생략할 수 있습니다.

(3) 커넥션 풀 종류
커넥션 풀은 직접 구현할 수 있지만, 검증된 오픈소스를 사용하는 것이 더 효율적입니다. 대표적인 오픈소스로는 "common-dbcp2", "tomcat-jdbc Pool", "DriverManager DataSource", "HikariCP" 등이 있습니다.

Java에서는 DataSource 인터페이스로 데이터베이스 연결 방법을 추상화했습니다. 각 커넥션 풀 오픈소스가 이를 구현하므로, DriverManager 사용 코드를 수정하지 않고도 전환할 수 있습니다.

Spring Boot 2.0 이전에는 Apache Commons DBCP를 주로 사용했으나, 2.0 이후부터는 HikariCP를 기본 DBCP로 채택했습니다.
```

- JDBC API를 사용하여 데이터베이스와 연결하기 위해 Connection 객체를 생성하는 작업은 비용이 굉장히 많이 드는 작업 중 하나입니다.
- Connection 객체를 생성하는 과정을 살펴보겠습니다!
- 이러한 문제를 해결하기 위해 애플리케이션 로딩 시점에 Connection 객체를 미리 생성하고, 애플리케이션에서 데이터베이스에 연결이 필요할 경우 미리 준비된 Connection 객체를 사용하여 애플리케이션의 성능을 향상하는 커넥션 풀(Connection Pool)이 등장하게 되었습니다.

!https://blog.kakaocdn.net/dn/dznP9K/btsslaU7nla/hrJp1eTKgcDLW58leq8ZG0/img.png

- 커넥션 풀은 단순해서 직접 구현할 수도 있지만, 사용이 편리하고 성능이 뛰어난 오픈소스 커넥션 풀이 많아 이를 사용하는 것이 좋습니다.
- 대표적인 커넥션 풀 오픈소스로는 "common-dbcp2", "tomcat-jdbc Pool", "DriverManager DataSource", "HikariCP" 등이 있습니다.
- Java에서 데이터베이스와 연결하는 방법을 추상화한 DataSource 인터페이스를 각각의 커넥션 풀 오픈소스에서 구현하기 때문에, 기존에 DriverManager 클래스를 이용하여 Connection 객체를 생성하여 사용했더라도 소스 코드 변경 없이 전환할 수 있습니다.
- Spring Boot 2.0 이전 버전에서는 Apache 재단의 오픈소스인 Apache Commons DBCP를 주로 사용했지만, Spring Boot 2.0 이후에는 HikariCP를 기본 DBCP로 채택하여 사용하고 있습니다.
