<pre><code>
SELECT * FROM ACCOUNTS, LOANS;

select e1.name as employee ,e2.name as manager from employees e1 left outer join employees e2 on e1.manager_id = e2.employee_id;

select e.name, d.department_name from employees e cross join departments d;

select * from employees NATURAL JOIN departments;

select c.name , a.account_type from customers c join accounts a on c.customer_id = a.customer_id;

select * from customers c join accounts a on c.customer_id = a.customer_id join transactions t on a.account_id = t.account_id;

select c.name, a.account_type from customers c left outer join accounts a on c.customer_id = a.customer_id;

select c.name as customer_name, b.name as branch_name from customers c cross join branches b;

select e.employee_id, e.name as emp_name, d.department_name, d.location 
from employees e, departments d 
where e.department_id = d.department_id(+) and d.location = 'New York';

--실습과제 7
select b.name as branch_name, c.name as customer_name, l.amount
from branches b 
left outer join loans l 
on b.branch_id = l.branch_id
left outer join customers c 
on l.customer_id = c.customer_id;

select c.name, c.email
from customers c 
join accounts a 
on c.customer_id = a.account_id 
where a.balance >= 50000;

select c.name, l.amount
from customers c 
join loans l 
on c.customer_id = l.customer_id 
where l.status = 'PENDING';





--------------------------------------------------------------
--1번
select e.name, d.location
from employees e join departments d
on e.department_id = d.department_id;

--2번
select e1.name as employee_name, e2.name as manager_name
from employees e1
left outer join employees e2
on e1.manager_id = e2.employee_id;

--3번
select name, NVL(TO_CHAR(salary), 'N/A') as salary
from employees;

--4번
select e.name, e.salary
from employees e
join departments d
on e.department_id = d.department_id
where d.location = 'Chicago';

--5번
select e.name, d.department_name
from employees e
left outer join departments d
on e.department_id = d.department_id
where e.salary between 5000 and 10000;

--6번
select name, hire_date
from employees
where hire_date > add_months(sysdate, -3);

--7번
select e.name, d.department_name
from departments d
join employees e
on d.manager_id = e.employee_id;

--8번
select job_id, count(job_id)
from employees
group by job_id;

--9번
select d.department_name, ceil(avg(e.salary)) as avg_salary
from departments d
join employees e
on d.department_id = e.department_id
group by d.department_name;

--10번
select sum(salary)
from employees;

--11번
select l.amount, c.name
from loans l
join customers c
on l.customer_id = c.customer_id
order by l.amount desc;

--12번
select status, count(distinct customer_id)
from loans
group by status;

select status, count(*)
from loans
group by status;

--13번
select *
from (
    select department_id, count(*) as cnt
    from employees e
    group by department_id
    having department_id is not null
    order by cnt
)
where rownum = 1;

--14번
select extract(month from created_at) || '월' as month, count(*)
from customers
group by extract(month from created_at)
order by month desc;

SELECT TO_CHAR(CREATED_AT, 'YYYY-MM') MONTHS, COUNT(CUSTOMER_ID) NEW_CUSTOMERS
FROM CUSTOMERS
GROUP BY TO_CHAR(CREATED_AT, 'YYYY-MM')
ORDER BY MONTHS DESC;

--15번
select s1.category_name, s2.category_name
from shopping_categories s1
left outer join shopping_categories s2
on s1.parent_category_id = s2.category_id;

--16번
select b.name,trunc(sum(l.amount))
from branches b
join loans l
on b.branch_id = l.branch_id
group by b.name
having b.name = 'Branch 8';

--17번
select job_id,
    CASE WHEN avg(salary) >= 8000 THEN 'HIGH'
         WHEN avg(salary) >= 6000 THEN 'MEDIUM'
         ELSE 'LOW'
         END AS grade
from employees
group by job_id
having avg(salary) >= 5000
order by avg(salary) desc;

--18번
select c.name, sum(t.amount)
from customers c
left outer join accounts a
on c.customer_id = a.customer_id
join transactions t
on a.account_id = t.account_id
group by c.name
order by c.name;

--19번
select d.department_name, round(avg(sysdate - hire_date), 0)
from departments d
join employees e
on d.department_id = e.department_id
group by d.department_name
order by d.department_name;

--20번
select *
from (
    select c.name, sum(l.amount)
    from customers c
    join loans l
    on c.customer_id = l.customer_id
    group by c.name
    order by sum(l.amount) desc, c.name)
where rownum < 6;

</code></pre>
