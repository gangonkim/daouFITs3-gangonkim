<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="example.MyBook">
    <select id='selectByISBN' parameterType="String" resultType="example.vo.BookVO">
        <!--SQL QUERY 안에 특수문자 태그로 인식되지 않도록 CDATA를 써줌-->
        <![CDATA[
            SELECT bisbn, btitle, bprice, bauthor
            FROM book
            WHERE bisbn = #{bisbn}
        ]]>
    </select>

    <select id='selectByKeyword' parameterType="String" resultType="example.vo.BookVO">
        <!-- SQL QUERY 안에 특수문자 태그로 인식되지 않도록 CDATA를 써줌 -->
        <![CDATA[
        SELECT bisbn, btitle, bprice, bauthor
        FROM book
        WHERE btitle LIKE '%' || #{keyword} || '%'
    ]]>
    </select>

    <insert id="insertBook" parameterType="example.vo.BookVO">
    <![CDATA[
        INSERT INTO book (bisbn, btitle, bprice, bauthor)
        VALUES (#{bisbn}, #{btitle}, #{bprice}, #{bauthor})
        ]]>
    </insert>

    <update id="updateBook" parameterType="example.vo.BookVO">
    <![CDATA[
        UPDATE book
        SET btitle = #{btitle},
            bprice = #{bprice},
            bauthor = #{bauthor}
        WHERE bisbn = #{bisbn}
        ]]>
    </update>

    <delete id="deleteBook" parameterType="String">
    <![CDATA[
        DELETE FROM book
        WHERE bisbn = #{bisbn}
        ]]>
    </delete>






</mapper>