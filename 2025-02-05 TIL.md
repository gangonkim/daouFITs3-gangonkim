# 1. class

class는 현실세계의 설계도와 같은 개념

- `ADT`(Abstract Data Type)
- 사용자 정의 데이터 타입
- 기존 데이터 타입을 이용해 새로운 데이터 타입을 만드는 것.

# 2. **instance**

class에 정의된 field와 method를 사용하기 위해서는 메모리에 일정 공간을 확보해야함

 이렇게 메모리상에 생성된 일정 공간이 class의 `instance`,혹은 객체(object)

Java program은 class와 그 class에서 파생된 instance를 가지고 동작.

# 3. **constructor**

Java의 모든 class는 적어도 하나 이상의 constructor를 가짐.

method와 비슷한 구조를 가지지만 리턴타입이 존재하지 않기 때문에 method는 아님

constructor의 이름은 class의 이름과 같아야 함.

객체가 생성될 때 `new` 키워드와 함께 호출되며 객체가 가지는 필드에 대한 초기화 등의 작업을 주로 함.

만약 생성자를 명시적으로 작성하지 않았다면 javac compiler에 의해서 default 생성자가 자동으로 추가됨. (default 생성자 : 인자를 가지지 않고 어떠한 일도 하지 않는 생성자를 의미.)

# 4. **JVM의 메모리 구조**

JVM은 메모리를 용도에 따라 다음의 5개의 부분으로 나누어서 관리

- Register
- Runtime Constant Pool
- Method Area
- Heap
- Call Stack( 일반적으로 Stack이라고 함 )

# 5. **class member & instance member**

`class member`는 static keyword로 선언.

class member는 모든 instance(객체)가 공유하는 field와 method로 class명으로 사용이 가능. instance를 이용해서 사용하는 것도 가능.

`instance member`는 각 instance가 독립적으로 메모리 공간을 확보하는 field와 method로 instance를 이용해야 사용가능.

class variable( static으로 지정된 field )의 사용은 객체의 존재 유무와 관련이 없음. class가 메모리에 load될 때 `Method Area`에 단 한번 class variable를 위한 메모리가 할당되고 초기화

따라서, 객체를 생성하지 않더라도 class variable에 대한 접근이 가능.

# 6. **method overloading**

같은 이름의 method가 여러 개 존재하는 것을 의미.

method가 갖는 매개변수의 개수와 데이터 타입을 이용하여 method의 이름은 같지만 서로 다른 method로 구분하는 개념.

method에 대한 overloading이 constructor에도 적용. (constructor overloading)

# 7. **package & import**

source code를 효율적으로 관리하고 재사용하는 방법

class를 package로 묶을 때에는 source code안에 package구문을 추가하면 . 다른 class를 사용할 때는 반드시 class의 package를 명시해야 class를 사용할 수 있는데 이를 편하게 사용하기 위한 구문이 바로 `import`

# 8. **access modifier (접근제어자)**

- `public` : 어디에서나 접근 가능한 field, method, constructor, class 선언에 사용
- `protected` : 기본적으로 default와 같지만 상속관계에 있는 class에서 field, method, constructor를 사용할 수 있도록 선언
- `default` : 같은 package내의 다른 class에서 접근 가능한 field, method, constructor, class 선언
- `private` : 다른 class에서는 접근할 수 없는 field, method, constructor 선언

# 9. **static block**

static block은 class가 로드된 후 main thread에 의해서 main method가 호출되기 이전에 실행

# 10. **inheritance**

- 상속은 부모 class가 가진 내용을 자식 class가 물려받는 것이다.
- 상속이 필요한 이유

상속은 어떻게 표현되나?

- 자식 클래스 선언 시 extends keyword를 이용하여 부모 클래스를 상속
- Java에서의 상속은 단일 상속만 지원, 다중상속 X
- 상속은 항상 좋은가?

클래스들은 결국 상속에 상속을 이어가면서 확장, 사용

자바의 모든 클래스는 상속관계에 있음
