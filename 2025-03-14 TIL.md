```
#include <stdio.h>
#include <string.h>

typedef struct {
    char name[50];  // 학생 이름
    int id;         // 학번
    int scores[3];  // 세 과목 점수
    float average;  // 평균 점수
} Student;


typedef struct {
    char title[50];
    char author[50];
    int year;
    char genre[10];
} Book;

#define MAX_BOOKS 3  // 최대 도서 수


//실습과제 28
struct game {
    char name[7];
    int R1, R2, R3
};


int main() {

    //실습과제 28
    struct game player;
    double avg;
    printf("선수의 이름은? ");
    scanf("%s", player.name);
    printf("1, 2, 3라운드 점수는? ");
    scanf("%d %d %d", &player.R1, &player.R2, &player.R3);
    avg = (double) (player.R1 + player.R2 + player.R3) / 3;
    printf("%s선수의 게임 결과 평균 %.1lf점 \n", player.name, avg);

    //실습과제 29
    struct student {
        char name[20];
        char sex;
        int stid;
        int sub1;
        int sub2;
        int sub3;
        double avg;
    } st1 = {"kdhong", "m", 1508001,98, 89, 92, 0.0}, st2;
    st1.avg = (st1.sub1 + st1.sub2 + st1.sub3) / 3.0;
    st2 = st1;
    strcpy(st2.name, "yhkim");
    st2.sex = "f";
    st2.stid = 1608024;
    printf("%s님(성별 : %s)의 학번은 %d이고, \n이번 시험 3과목의 평균은 %5,2f입니다 \n", st2.name, st2.sex == "m"?"남자":"여자", st2.stid, st2.avg);

    // Student students[2];  // 학생 2명의 정보를 저장할 배열
    //
    // for (int i = 0; i < 2; i++) {
    //     printf("학생 %d의 정보 입력:\n", i + 1);
    //
    //     printf("이름: ");
    //     scanf("%s", students[i].name);  // 학생 이름 입력
    //
    //     printf("학번: ");
    //     scanf("%d", &students[i].id);  // 학번 입력
    //
    //     // 세 과목 점수 입력
    //     for (int j = 0; j < 3; j++) {
    //         printf("과목 %d 성적: ", j + 1);
    //         scanf("%d", &students[i].scores[j]);
    //     }
    //
    //     // 평균 점수 계산
    //     students[i].average = (students[i].scores[0] + students[i].scores[1] + students[i].scores[2]) / 3.0;
    // }
    //
    // // 학생들의 정보 출력
    // printf("\n... 학생 성적 정보:\n");
    // for (int i = 0; i < 2; i++) {
    //     printf("이름: %s, 학번: %d, 평균: %.2f\n", students[i].name, students[i].id, students[i].average);
    // }


    Book books[MAX_BOOKS];  // 도서 배열 선언
    int bookCount = 0;      // 입력된 도서의 개수
    char searchGenre[30];   // 검색할 장르

    // 도서 정보 입력 받기
    //%[^\n]로 받으면 공백도 받을수있음
    for (int i = 0; i < MAX_BOOKS; i++) {
        printf("도서 %d의 정보 입력:\n", i + 1);
        printf("제목: ");
        scanf(" %[^\n]", books[i].title);  // 제목 입력 (공백 포함)
        printf("저자: ");
        scanf(" %[^\n]", books[i].author);  // 저자 입력 (공백 포함)
        printf("출판년도: ");
        scanf("%d", &books[i].year);      // 출판년도 입력
        printf("장르: ");
        scanf(" %[^\n]", books[i].genre);  // 장르 입력 (공백 포함)

        bookCount++;  // 도서 수 증가
    }

    // 장르 입력 받기
    printf("\n장르를 입력하세요: ");
    scanf(" %[^\n]", searchGenre);  // 검색할 장르 입력

    // 해당 장르의 도서 출력
    printf("\n'%s' 장르의 책들:\n", searchGenre);
    int found = 0;  // 해당 장르의 책이 있는지 확인

    //strcmp: 문자열 비교 함수
    //두 문자열이 일치하면 0을 반환
    //str1이 str2보다 사전순으로 앞에 있으면 음수를 반환
    //str1이 str2보다 사전순으로 뒤에 있으면 양수를 반환
    for (int i = 0; i < bookCount; i++) {
        if (strcmp(books[i].genre, searchGenre) == 0) {
            printf("제목: %s, 저자: %s, 출판년도: %d\n", books[i].title, books[i].author, books[i].year);
            found = 1;  // 해당 장르의 책을 찾았음을 표시
        }
    }

    if (!found) {
        printf("해당 장르의 책이 없습니다.\n");
    }


    return 0;
}

```
