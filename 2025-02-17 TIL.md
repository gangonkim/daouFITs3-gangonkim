1. 웹 프로젝트 생성(JDK, TOMCAT, OJDBC 버전 맞춰줘야함)
2. Servlet은 인터페이스. 이 Servlet 인터페이스를 직접적으로 구현하는 것이 아니라 Servlet, GenericServlet class - HttpServlet class 가 있음

상속되는 메서드

- init() : 서블릿 객체가 생성될 때 호출되는 초기화를 담당하는 메서드(생성자 대신 init()를 이용해서 초기화를 진행)
- service(): 클라이언트가 서블릿을 실행할 때 호출되는 메서드. 이 서비스는 이미 하는 일이 구현되어 있음 → 일반적으로 오버라이딩 하지 않음
- 이 메서드는 클라이언트의 요청이  Get방식이면 doGet()을 호출하고 POST방식이면 doPost()를 호출하는 것으로 구현되어 있음
- doGet(), doPost() 이 두 개의 메서드를 오버라이딩해서 원하는 작업을 수행하면 됨
- destory(): 서블릿 인스턴스가 WAS에서 삭제될 때 수행되는 메서드. 사용하고 있는 여러 리소스들을 해제해줘야함

클라이언트가 서블릿을 호출하기 위해 서블릿에 논리적인 이름을 부여, 이를 이용해 호출

1. xml파일을 이용해서 설정가능 → 좀번거로움, but전체적인 이름 관리를 하나의 파일에서 할 수 있다는 장점
2. 어노테이션을 이용해서 이름을 설정할 수 있음(url 매핑) → 일반적인 방식, 직관적으로 사용할 수 있음

데이터 전달 방식

1. GET방식: QueryString을 이용해서 데이터 전달
    
    QueryString → 특수한 형식을 가진 문자열
    
    ? 변수 = 값 & 변수 = 값 & …
    
    이렇게 전달된 데이터를 getparameter로 받음
    
    보안성이 전혀 없음(128bit암호화를 해야함)
    
    url길이 제한으로 많은 양의 데이터를 보낼 수 없음
    
2. POST방식 
    
    html의 특수한 태그를 이용해서 서버쪽 프로그램(서블릿)을 호출하는 구조 
    
    브라우저 주소창에 변수와 값에 대한 내용이 표현되지 않음
    
    request header에 포함되어서 전달되기 때문에 길이의 제한이 없음
