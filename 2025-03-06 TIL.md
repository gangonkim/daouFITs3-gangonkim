```
#include <stdio.h>
#include <stdint.h>

uint16_t rightRotate(uint16_t value) {
	// 오른쪽으로 1비트 이동하고, 밀려나가는 비트를 최상위 비트에 채워 넣음
	return (value >> 1) | (value << 15);
}

int main() {
	
	//실습과제6-1
	char gender;
	int age;
	int height;
	printf("성별은?\n");
	scanf_s("%c", &gender, 1);
	getchar(); // 버퍼 비우기
	printf("나이는?\n");
	scanf_s("%d", &age);
	printf("키는?\n");
	scanf_s("%d", &height);
	printf("성별: %c, 나이: %d, 키: %d\n", gender, age, height);

	//실습과제6-2
	char name[10] = {0}; // 초기화
	printf("이름은?\n");
	//gets(name); // gets 함수는 사용하지 않는 것이 좋음
	fgets(name, sizeof(name), stdin); // gets 대신 fgets 사용
	name[strcspn(name, "\n")] = 0; // 개행 문자 제거
	puts(name);

	char gender1;
	printf("성별은?\n");
	gender1 = getchar();
	printf("성별: %c\n", gender1);
	scanf_s("%s", name, sizeof(name));
	printf("이름: %s\n", name);

	//실습과제6-3
	int int1;
	int int2;
	printf("두 정수를 입력하세요\n");
	scanf_s("%d %d", &int1, &int2);
	printf("입력한 두 정수: %d, %d\n", int1, int2);

	char isodd[10]  = { 0 };
	int a;
	scanf_s("%d", &a);
	// 삼항 연산자를 사용하여 홀수와 짝수를 판별
	const char* result = (a % 2 == 0) ? "짝수" : "홀수";
	printf("%d는 %s입니다.\n", a, result);


	//실습과제 7
	char gender;
	int age;
	int height;
	printf("성별은?\n");
	scanf_s("%c", &gender, 1);
	getchar(); // 버퍼 비우기
	printf("나이는?\n");
	scanf_s("%d", &age);
	printf("키는?\n");
	scanf_s("%d", &height);
	printf("성별: %c, 나이: %d, 키: %d\n", gender, age, height);

	//실습과제 8
	char name[10];
	int year;
	double GPA, score;

	printf("이름은?\n");
	gets(name);
	printf("학년은?\n");
	scanf_s("%d", &year);
	printf("%d학년 평점은?", year);
	scanf("%lf", &GPA);
	score = (GPA * 100) / 4.5;
	printf("\n---------------------------");
	printf("  이름   학년    평점    (점수)");
	printf("\n---------------------------");
	printf("%-10s %d %7.1f (%5.1f)", name, year, GPA, score);
	printf("\n---------------------------");

	//실습과제9-1

	int n1, n2, max, min;
	printf("두 정수를 입력하세요: ");
	scanf_s("%d %d", &n1, &n2);
	(n1 > n2) ? (max = n1, min = n2) : (max = n2, min = n1);
	printf("큰 수 / 작은 수: %d\n", max / min);
	printf("나머지: %d\n", max % min);


	//실습과제9-2
	int x = 1, y = 2, z = 3;

	x = ++x * 5;
	y = y++ * 5;
	z = 5 - --z;
	printf("x=%d, y=%d, z=%d\n", x, y, z);



	int x = 15, y = 21;
	printf("%d\n", x & y);
	printf("%d\n", x | y);
	printf("%d\n", x ^ y);



	//문1: =은 대입연산자, ==은 비교연산자
	//문2: 증감 > 비트 > 산술 > 관계 > 논리 > 조건 > 대입 > 콤마
	//문3: 


	//문4 킬로그램 단위의 무게를 입력받아 파운드(lb)단위로 출력하는 프로그램을 작성하시오 
	double weight;
	printf("킬로그램 단위의 무게를 입력하세요: ");
	scanf_s("%lf", &weight);
	printf("파운드 단위의 무게: %.2f\n", weight * 2.2046);

	////문5 원기둥의 밑면의 반지름과 높이를 입력받아 부피를 계산하는 프로그램을 작성하시오
	double radius, height;
	printf("반지름과 높이를 입력하세요: ");
	scanf_s("%lf %lf", &radius, &height);
	printf("원기둥의 부피: %.2f\n", 3.141592 * radius * radius * height);

	//문6 키보드로 월을 입력 받아 입력 받은 월이 상반지인지 하반지인지 판단하여 출력하는 프로그램을 작성하시오.
	printf("월을 입력하세요: ");
	int month;
	scanf_s("%d", &month);
	const char* result = (month >= 1 && month <= 6) ? "상반기" : "하반기";
	printf("%d월은 %s입니다.\n", month, result);

	//문7 기가바이트 수치를 입력받아 입력한 값이 몇 메가바이트인지, 몇 킬로바이트인지 계산하는 프로그램을 작성하시오.
	double gigabyte;
	printf("기가바이트 수치를 입력하세요: ");
	scanf_s("%lf", &gigabyte);
	printf("입력한 값은 %.2f메가바이트, %.2f킬로바이트입니다.\n", gigabyte * 1024, gigabyte * 1024 * 1024);

	//문8 초 단위의 시간을 입력 받아 몇시간 몇분 몇초인지를 계산하는 프로그램을 작성하시오
	int time;
	printf("초 단위의 시간을 입력하세요: ");
	scanf_s("%d", &time);
	int hour = time / 3600;
	int minute = (time % 3600) / 60;
	int second = (time % 3600) % 60;
	printf("%d시간 %d분 %d초\n", hour, minute, second);

	//문9 다음 수식을 계산하는 프로그램 작성하시오
	//f(x) - 2x^3 – 3x^2 + 1/x
	double x;
	printf("x값을 입력하세요: ");
	scanf_s("%lf", &x);
	printf("f(x) = %.2f\n", 2 * x * x * x - 3 * x * x + 1 / x);
	
	//문10 조건부 연산자를 이용해서 정수 3개를 입력 받아 가장 큰 수를 출력하세요
	int a, b, c;
	printf("정수 3개를 입력하세요: ");
	scanf_s("%d %d %d", &a, &b, &c);
	int max = (a > b) ? (a > c ? a : c) : (b > c ? b : c);
	printf("가장 큰 수: %d\n", max);
	
	//6. 입력받은 문자의 비트열 구하기
	char ch;
	printf("문자를 입력하세요: ");
	scanf_s("%c", &ch, 1);
	printf("%c의 비트열: ", ch);
	for (int i = 7; i >= 0; i--) {
		printf("%d", (ch >> i) & 1);
	}
	printf("\n");

	uint16_t num;
	printf("16비트 부호 없는 정수형 데이터를 입력하세요: ");
	scanf_s("%hd", &num);

	uint16_t rotated = rightRotate(num);
	for (int i = 15; i >= 0; i--) {
		printf("%d", (rotated >> i) & 1);
	}
	return 0;

	//6페이지 20. 조건 연산자를 사용해서 4개 정수의 최대값, 최소값을 출력하시오
	int a, b, c, d;
	printf("정수 4개를 입력하세요: ");
	scanf_s("%d %d %d %d", &a, &b, &c, &d);
	int max = (a > b) ? (a > c ? (a > d ? a : d) : (c > d ? c : d)) : (b > c ? (b > d ? b : d) : (c > d ? c : d));
	int min = (a < b) ? (a < c ? (a < d ? a : d) : (c < d ? c : d)) : (b < c ? (b < d ? b : d) : (c < d ? c : d));
	printf("최대값: %d, 최소값: %d\n", max, min);

	//7페이지 2. 키보드에서 8진수 정수와 16진수 정수를 하나씩 입력 받은 후, 이 입력 값들과 두 정수의 합을 10진 정수로 출력하는 프로그램을 작성하시오
	int oct, hex;
	printf("8진수와 16진수 정수를 입력하세요: ");
	scanf_s("%o %x", &oct, &hex);
	printf("8진수: %d, 16진수: %d, 합: %d\n", oct, hex, oct + hex);

	//7페이지 4. 키와 몸무게를 입력 받아 키에 맞는 표준 체중을 구한 후 자신의 몸무게와 표준 체중과의 차이를 출력하는 프로그램을 작성하시오
	//힌트 표준체중 = (자신의 키 – 100) * 0.9 표준체중과의 차이 = 자신의 몸무게 – 표준체중
	//1kg = 2.20459 파운드                                                                                            
	//1feet = 30.49cm
	double height, weight;
	printf("키와 몸무게를 입력하세요: ");
	scanf_s("%lf %lf", &height, &weight);
	double standard = (height - 100) * 0.9;
	printf("표준 체중: %.2f, 차이: %.2f\n", standard, weight - standard);

	
	//8페이지 9. 학생의 이름(이름 사이에 띄어쓰기 가능)과 국어, 영어, 수학, 출결점수를 키보드로 입력 받아 네 과목 점수의 평균(소수 1자리)을 구하고 합격 여부(평균 70점 이상이고 출결점수가 80점 이상인 경우에만 합격)을 출력하는 프로그램을 작성하시오
	char name[20];
	int kor, eng, math, attend;
	printf("이름, 국어, 영어, 수학, 출결점수를 입력하세요: ");
	scanf_s("%s %d %d %d %d", name, sizeof(name), &kor, &eng, &math, &attend);
	double average = (kor + eng + math + attend) / 4.0;
	const char* result = (average >= 70 && attend >= 80) ? "합격" : "불합격";
	printf("평균: %.1f, 합격 여부: %s\n", average, result);

	//14. 다음과 같이 몇 가지 특수 기호들을 번호 선택에 의해 출력하는 프로그램을 작성해 보세요.
	// 특수기호 출력 1:★ 2 : ◆ 3 : ■ 4 : ▲ 5 : ♠ 6 : ♥ 7 : ♣
	int num;
	printf("1~7 사이의 숫자를 입력하세요: ");
	scanf_s("%d", &num);
	switch (num) {
	case 1:
		printf("★\n");
		break;
	case 2:
		printf("◆\n");
		break;
	case 3:
		printf("■\n");
		break;
	case 4:
		printf("▲\n");
		break;
	case 5:
		printf("♠\n");
		break;
	case 6:
		printf("♥\n");
		break;
	case 7:
		printf("♣\n");
		break;
	default:
		printf("잘못된 입력입니다.\n");
		break;
	}

	//문) printf(“*”)문장을 이용하여 다음과 같이 출력해 보세요
	//****
	//***
	//**
	//*

	for (int i = 4; i > 0; i--) {
		for (int j = 0; j < i; j++) {
			printf("*");
		}
		printf("\n");
	}*/

	//문) 정수가 999가 아닐 때까지 계속 입력 받기
	//int num = 0;
	//while (num != 999) {
	//	printf("정수를 입력하세요: ");
	//	scanf_s("%d", &num);
	//}

	//실습과제 10
	//문) ========================
	//1. 원의 둘레 구하기
	//2. 원의 넓이 구하기
	//3. 구의 부피 구하기
	//4. 그만두기
	//======================== 원하는 메뉴는 ? 2
	//>> 반지름은 ? 7
	//>> 반지름이 7인 원의 넓이는 153.94

	printf("=======================\n");
	printf("1. 원의 둘레 구하기\n");
	printf("2. 원의 넓이 구하기\n");
	printf("3. 구의 부피 구하기\n");
	printf("4. 그만두기\n");
	printf("=======================\n");
	int menu;
	printf("원하는 메뉴는? ");
	scanf_s("%d", &menu);
	switch (menu) {
	case 1:
		printf("원의 둘레 구하기\n");
		printf("반지름은? ");
		double radius;
		scanf_s("%lf", &radius);
		printf("반지름이 %.2f인 원의 둘레는 %.2f\n", radius, 2 * 3.141592 * radius);
		break;
	case 2:
		printf("원의 넓이 구하기\n");
		printf("반지름은? ");
		double radius1;
		scanf_s("%lf", &radius1);
		printf("반지름이 %.2f인 원의 넓이는 %.2f\n", radius1, 3.141592 * radius1 * radius1);
		break;
	case 3:
		printf("구의 부피 구하기\n");
		printf("반지름은? ");
		double radius2;
		scanf_s("%lf", &radius2);
		printf("반지름이 %.2f인 구의 부피는 %.2f\n", radius2, 4 / 3 * 3.141592 * radius2 * radius2 * radius2);
		break;
	case 4:
		printf("프로그램을 종료합니다.\n");
		break;
	}


	//4. 별 모습 찍기
	//   *
	//	***
	// *****
	//*******

	for (int i = 0; i < 4; i++) {
		for (int j = 0; j < 3 - i; j++) {
			printf(" ");
		}
		for (int j = 0; j < 2 * i + 1; j++) {
			printf("*");
		}
		printf("\n");
	}

	//6. 결과 출력
	//A
	//AB
	//ABC
	//ABCD
	//...
	//ABCDEF... Z

	for (int i = 0; i < 26; i++) {
		for (int j = 0; j <= i; j++) {
			printf("%c", 'A' + j);
		}
		printf("\n");
	}

	//10. 국어, 영어, 수학 점수를 입력 받아서 총점, 평균을 구하고 평균에 맞는 학점을 구하시오. 평균은 소수점 2자리까지 구하고, ‘N’이 입력될 때까지 반복 수행하시오

	char continueInput = 'Y';

	while (continueInput != 'N' && continueInput != 'n') {
		int kor, eng, math;
		printf("국어, 영어, 수학 점수를 입력하세요: ");
		scanf_s("%d %d %d", &kor, &eng, &math);

		double total = kor + eng + math;
		double average = total / 3;
		char grade;

		if (average >= 90) {
			grade = 'A';
		}
		else if (average >= 80) {
			grade = 'B';
		}
		else if (average >= 70) {
			grade = 'C';
		}
		else if (average >= 60) {
			grade = 'D';
		}
		else {
			grade = 'F';
		}

		printf("총점: %.2f, 평균: %.2f, 학점: %c\n", total, average, grade);

		printf("계속 입력하시겠습니까? (N/n을 입력하면 종료): ");
		scanf_s(" %c", &continueInput, 1);
	}

	//10. 정수를 입력받아서 그 정수를 표현하는데 필요한 유효 비트 수를 출력하는 프로그램을 작성하시오
	//힌트 진법변환

	int num;
	printf("정수를 입력하세요: ");
	scanf_s("%d", &num);

	int validBits = 0;
	for (int i = 31; i >= 0; i--) {
		if ((num >> i) & 1) {
			validBits = i + 1;
			break;
		}
	}

	printf("입력한 정수를 표현하는데 필요한 유효 비트 수: %d\n", validBits);

	int n;
	printf("정수 n을 입력하세요: ");
	scanf_s("%d", &n);

	int sum = 0;
	for (int i = 1; i <= n; i++) {
		if (i % 2 == 0) {
			sum += i;
		}
	}

	printf("1에서 %d 이하 짝수들의 합: %d\n", n, sum);

}

```
