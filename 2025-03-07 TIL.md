1. 함수의 종류
    - 표준함수: 내장함수 또는 시스템 제공 함수라고도 하며, 프로그래머가 호출하여 사용할 수 있도록 미리 정의되어 있는 함수
    - 헤더파일: C언어에서 미리 만들어 놓은 함수들이 정의되어 있는 파일
    - 사용자 정의 함수: 사용자가 특정 작업을 수행하기 위해 필요에 따라 직접 정의하고 작성하는 함수
2. 함수의 구조
매개변수를 사용하여 전달하기 - 값으로 전달(Call by Value)리턴타입 함수명(자료형 인수1, 자료형 인수2)    
    
    // 1단계 함수 원형 선언void main() {
    // 2단계 함수 호출
       변수명 = 함수명(인수1, 인수2);
    }리턴타입 함수명(자료형 인수1, 인수2)    
    
    // 3단계 함수 선언부
    {
        함수 본문;
        return value;
    }
```
#include <stdio.h>
#include <conio.h>
#include <math.h>

int func(int n1, int n2, int n3) {
	return n1 + n2 + n3;
}

//C++에서는 메서드오버로딩 가능
int func(int n1, int n2) {
	return n1 + n2;
}


//1. 0도부터 180도까지 사인 함수의 값을 출력하시오

int sinfunc() {
	for (int i = 0; i <= 180; i++) {
		if (i % 30 == 0)
		printf("sin(%d) = %lf\n", i, sin(i * 3.14 / 180.0));
	}
	return 0;
}

// 2. 근로자의 오늘 하루 근로시간에 대한 하루 임금을 임금 기준에 따라 시급제와 일급제를 혼영하여 계산하시오
//임금기준
//시급은 10000원이며 8시간 이상 일하면 일급으로 100000원을 적용한다
//일급을 적용할 수 없는 나머지 시간을 시급을 적용한다.즉 10시간 일하면 8시간은 일급으로
//나머지 2시간은 시급으로 계산한다.

int calc() {
	int time;
	int wage = 10000;
	int dailywage = 100000;
	int totalwage = 0;
	printf("근로자의 오늘 하루 근로시간을 입력하세요 : ");
	scanf_s("%d", &time);
	if (time >= 8) {
		totalwage = dailywage + (time - 8) * wage;
	}
	else {
		totalwage = time * wage;
	}
	printf("근로자의 오늘 하루 임금은 %d원입니다.\n", totalwage);
	return 0;
}

//3. 2025년 3월 달력을 출력하시오
int calendar() {
	int year = 2025;
	int month = 3;
	int day = 1;
	int totalday = 0;
	int dayofweek = 0;
	int lastday = 0;
	const char* dayofweekstr[7] = { "일", "월", "화", "수", "목", "금", "토" };
	int dayofmonth[12] = { 31,28,31,30,31,30,31,31,30,31,30,31 };
	for (int i = 1; i < year; i++) {
		if (i % 4 == 0 && i % 100 != 0 || i % 400 == 0) {
			totalday += 366;
		}
		else {
			totalday += 365;
		}
	}
	for (int i = 0; i < month - 1; i++) {
		totalday += dayofmonth[i];
	}
	if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
		dayofmonth[1] = 29;
	}
	lastday = dayofmonth[month - 1];
	dayofweek = totalday % 7;
	printf("2025년 3월\n");
	for (int i = 0; i < 7; i++) {
		printf("%s\t", dayofweekstr[i]);
	}
	printf("\n");
	for (int i = 0; i < dayofweek; i++) {
		printf("\t");
	}
	for (int i = 1; i <= lastday; i++) {
		printf("%d\t", i);
		if ((i + dayofweek) % 7 == 0) {
			printf("\n");
		}
	}
	return 0;
}

//4. 사용자가 입력한 연도가 365일까지 있는지 366일까지 있는지를 출력하시오
int leapyear() {
	int year;
	printf("연도를 입력하세요 : ");
	scanf_s("%d", &year);
	if (year % 4 == 0 && year % 100 != 0 || year % 400 == 0) {
		printf("%d년은 366일입니다.\n", year);
	}
	else {
		printf("%d년은 365일입니다.\n", year);
	}
	return 0;
}

//배열을 인자로 받아 정렬하는 함수
void sort(int* arr, int size) {
	int temp;
	for (int i = 0; i < size - 1; i++) {
		for (int j = 0; j < size - 1 - i; j++) {
			if (arr[j] > arr[j + 1]) {
				temp = arr[j];
				arr[j] = arr[j + 1];
				arr[j + 1] = temp;
			}
		}
	}
}

// 6. 재귀함수를 이용해서 1~n까지의 합을 구하세요.
int sum(int n) {
	if (n == 1) {
		return 1;
	}
	else {
		return n + sum(n - 1);
	}
}

//7. n!을 구하시오.
int factorial(int n) {
	if (n == 1) {
		return 1;
	}
	else {
		return n * factorial(n - 1);
	}
}

//8. 피보나치 수열을 구하시오
int fibonacci(int n) {
	if (n == 0) {
		return 0;
	}
	else if (n == 1) {
		return 1;
	}
	else {
		return fibonacci(n - 1) + fibonacci(n - 2);
	}
}

//지수값 계산하기
int power(int n, int m) {
	if (m == 0) {
		return 1;
	}
	else {
		return n * power(n, m - 1);
	}
}

//함수를 사용해서 계산기 프로그램을 작성하세요
int calculator(int n1, int n2) {
	printf("두 수의 합은 %d입니다.\n", n1 + n2);
	printf("두 수의 차는 %d입니다.\n", n1 - n2);
	printf("두 수의 곱은 %d입니다.\n", n1 * n2);
	printf("두 수의 나눗셈은 %d입니다.\n", n1 / n2);
	return 0;
}

//넓이가 100cm2인 사각형 종이를 반으로 접을 때 최초로 1cm2보다 작아지는 횟수를 계산하는 함수
//재귀함수를 이용
int fold(int n, int count) {
	if (n <= 1) {
		return count;
	}
	else {
		return fold(n / 2, count + 1);
	}
}



int main() {


	//실습과제11-1
	int n1, n2;
	printf("가감승제를 원하는 두 수를 입력하세요: ");
	scanf_s("%d %d", &n1, &n2);
	calculator(n1, n2);

	//실습과제11-2
	int dan;
	printf("원하는 단을 입력하세요 : ");
	scanf_s("%d", &dan);
	for (int i = 1; i <= 9; i++) {
		printf("%d * %d = %d\n", dan, i, dan * i);
	}
	
	//실습과제 11-3
	//함수를 이용하여 대문자는 소문자로 소문자는 대문자로 변환하는 프로그램 작성
	char alphabet;
	printf("알파벳을 입력하세요 : ");
	scanf_s(" %c", &alphabet, 1);
	if (alphabet >= 'A' && alphabet <= 'Z') {
		printf("입력한 대문자의 소문자는 %c입니다.\n", alphabet + 32);
	}
	else if (alphabet >= 'a' && alphabet <= 'z') {
		printf("입력한 소문자의 대문자는 %c입니다.\n", alphabet - 32);
	}
	else {
		printf("알파벳이 아닙니다.\n");
	}


	//sinfunc();
	//calc();
	//calendar();
	//leapyear();

	/*int arr[] = {1, 2, 3, 4, 5};
	sort(arr, 5);
	for (int i = 0; i < 5; i++) {
		printf("%d ", arr[i]);
	}*/

	
	// 1. 자신의 가산점을 확인할 수 있는 프로그램을 작성하시오
	//가산점을 부여하는 규칙은 
	// 여자의 가산점 부여 규칙 :
	// 기혼자라면 + 1  기혼자로서 자녀수가 1이면 + 1, 자녀수가 2이면 + 2
	// 남자의 가산점 부여 규칙 :
	// 군제대자라면 + 1, 군제대자로서 기혼자라면 + 1

	/*char gender;
	char married;
	int children;
	char military;
	int score = 0;

	printf("성별을 입력하세요 (M/F) : ");
	scanf_s(" %c", &gender, 1);
	if (gender == 'F') {
		printf("결혼 여부를 입력하세요 (Y/N) : ");
		scanf_s(" %c", &married, 1);
		if (married == 'Y') {
			score += 1;
			printf("자녀수를 입력하세요 : ");
			scanf_s(" %d", &children, 1);
			if (children == 1) {
				score += 1;
			}
			else if (children == 2) {
				score += 2;
			}
		}
	}
	else {
		printf("군제대 여부를 입력하세요 (Y/N) : ");
		scanf_s(" %c", &military, 1);
		if (military == 'Y') {
			score += 1;
			printf("결혼 여부를 입력하세요 (Y/N) : ");
			scanf_s(" %c", &married, 1);
			if (married == 'Y') {
				score += 1;
			}
		}
	}

	printf("당신의 가산점은 %d점 입니다.\n", score);*/



	// 2. 수열의 합을 구하시오
	
	/*int n;
	double sum = 0;
	printf("임의의 정수 n을 입력하세요 : ");
	scanf_s("%d", &n);
	for (double i = 2; i <= n; i++) {
		sum += (i-1)/i;
	}

	printf("수열의 합은 %lf입니다.\n", sum);*/

	//3. 학생 10명의 퀴즈 점수를 입력받은 후, 전체 평균과 통과자수, 탈락자수를 구해 출력하시오.점수가 70점 이상이면 합격이다.

	/*int score[10];
	int pass = 0;
	int fail = 0;
	double average = 0;
	for (int i = 0; i < 10; i++) {
		printf("학생 %d의 퀴즈 점수를 입력하세요 : ", i + 1);
		scanf_s("%d", &score[i]);
		average += score[i];
		if (score[i] >= 70) {
			pass++;
		}
		else {
			fail++;
		}
	}
	average /= 10;
	printf("전체 평균은 %lf입니다.\n", average);
	printf("합격자는 %d명이고, 탈락자는 %d명입니다.\n", pass, fail);*/

	//4. 넓이가 100cm2인 사각형 종이를 반으로 접을 때 최초로 1cm2보다 작아지는 횟수를 확인할 수 있도록 넓이 변화를 출력하시오

	/*double area = 100;
	int count = 0;
	while (area > 1) {
		area /= 2;
		count++;
		printf("%d번째 접힐 때 넓이는 %.2lfcm2입니다.\n", count, area);
	}*/

	// 5. 특정 단의 구구단 출력하기

	/*int dan;
	printf("원하는 단을 입력하세요 : ");
	scanf_s("%d", &dan);
	for (int i = 1; i <= 9; i++) {
		printf("%d * %d = %d\n", dan, i, dan * i);
	}*/

	// 6. 주사위를 두 개 던졌을 때 합이 7이 되는 경우의 수를 출력하시오

	/*printf("--------------------\n");
	printf("주사위1 주사위2\n");
	printf("--------------------\n");
	for (int i = 1; i <= 6; i++) {
		for (int j = 1; j <= 6; j++) {
			if (i + j == 7) {
				printf("  %d      %d\n", i, j);
			}
		}
	}*/

	//7. 알파벳 출력하기

	/*char alphabet[27] = { 0, };
	int index = 0;
	for (char i = 'z'; i >= 'a'; i--) {
		alphabet[index++] = i;
		printf("%s \n", alphabet);
	}*/

	//8. 1~n까지의 곱을 구합니다

	/*int n;
	int result = 1;
	printf("임의의 정수 n을 입력하세요 : ");
	scanf_s("%d", &n);
	for (int i = 1; i <= n; i++) {
		result *= i;
	}
	printf("1부터 %d까지의 곱은 %d입니다.\n", n, result);*/

	//9. 인의 반감기는 14일이다. 연구소에 들어온 실험 물질에 포함된 인의 양은 100mg이었다. 앞으로 n일후 인의 양이 최소 몇 mg일지를 구하는 프로그램을 작성하시오

	/*int n;
	double amount = 100;
	printf("몇일째 되는 날의 인의 최소잔량을 알고 싶나요??: ");
	scanf_s("%d", &n);
	for (int i = 0; i < n; i++) {
		if (i % 14 == 0) {
			amount /= 2;
		}
	}
	printf("%d일 후의 인의 최소잔량은 %lfmg입니다.\n", n, amount);*/


	//10. 알파벳 소문자를 입력받아 대문자로 바꾸는 프로그램을 작성하시오. 입력된 문자가 소문자가 아니면 다시 문자를 입력받고 ESC키(키값은 Ox1b)가 눌리면 프로그램을 종료하시오

	/*char alphabet;
	while (1) {
		printf("알파벳 소문자를 입력하세요 : ");
		alphabet = _getch();
		if (alphabet == 27) {
			break;
		}
		while (getchar() != '\n');

		if (alphabet < 'a' || alphabet > 'z') {
			printf("소문자가 아닙니다. 다시 입력하세요.\n");
			continue;
		}
		printf("입력한 소문자의 대문자는 %c입니다.\n", alphabet - 32);
	}*/

	//11. 키보드로 태어난 년도를 입력받아 그해의 띠를 출력하는 프로그램을 switch~case문으로 작성하시오

	/*int year;
	printf("태어난 년도를 입력하세요 : ");
	scanf_s("%d", &year);
	switch ((year - 4) % 12) {
	case 0:
		printf("당신은 쥐띠 입니다.\n");
		break;
	case 1:
		printf("당신은 소띠 입니다.\n");
		break;
	case 2:
		printf("당신은 호랑이띠 입니다.\n");
		break;
	case 3:
		printf("당신은 토끼띠 입니다.\n");
		break;
	case 4:
		printf("당신은 용띠 입니다.\n");
		break;
	case 5:
		printf("당신은 뱀띠 입니다.\n");
		break;
	case 6:
		printf("당신은 말띠 입니다.\n");
		break;
	case 7:
		printf("당신은 양띠 입니다.\n");
		break;
	case 8:
		printf("당신은 원숭이띠 입니다.\n");
		break;
	case 9:
		printf("당신은 닭띠 입니다.\n");
		break;
	case 10:
		printf("당신은 개띠 입니다.\n");
		break;
	case 11:
		printf("당신은 돼지띠 입니다.\n");
		break;
	}*/

	//12. 키보드로 정수 하나와 계산방법을 입력 받은 뒤 계산 방법이 1이면 입력된 수를, 2이면 배수를, 3이면 3배수를 출력하는 프로그램을 switch~case문으로 작성하시오.
	
	/*int num;
	int method;
	printf("정수 하나를 입력하세요 : ");
	scanf_s("%d", &num);
	printf("계산 방법을 입력하세요 (1: 입력된 수, 2: 배수, 3: 3배수) : ");
	scanf_s("%d", &method);
	switch (method) {
	case 1:
		printf("입력된 수는 %d입니다.\n", num);
		break;
	case 2:
		printf("입력된 수의 배수는 %d입니다.\n", num * 2);
		break;
	case 3:
		printf("입력된 수의 3배수는 %d입니다.\n", num * 3);
		break;
	}*/

	//22. 자연수 두 개를 입력받은 후 최대 공약수를 구하는 프로그램을 작성하시오
	
	/*int num1, num2;
	int gcd;
	printf("자연수 두 개를 입력하세요 : ");
	scanf_s("%d %d", &num1, &num2);
	for (int i = 1; i <= num1 && i <= num2; i++) {
		if (num1 % i == 0 && num2 % i == 0) {
			gcd = i;
		}
	}
	printf("두 수의 최대 공약수는 %d입니다.\n", gcd);*/


	/*printf("정수 3개를 입력하세요 : ");
	int n1, n2, n3;
	scanf_s("%d %d %d", &n1, &n2, &n3);
	printf("세 수의 합은 %d입니다.\n", func(n1, n2, n3));*/

	/*sum(5 ,6 ,11);*/

	
    /*int* a;  
    int b = 10;  
    a = &b;
	printf("%p", *a);*/






	return 0;
}

```
