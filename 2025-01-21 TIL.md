partition by:

- 데이터를 특정 그룹으로 나눔
- 그룹별로 윈도우 함수가 작동

order by:

- 데이터를 정렬하여 윈도우 함수의 동작 순서를 지정
- 누적 계산에 주로 사용

### 윈도우 함수 옵션

- 윈도우 함수에서 특정 행이나 범위를 지정하는 추가 설정
- 옵션을 활용해 데이터 처리 범위를 유연하게 조정 가능
- 주요 옵션:
    - ROWS
    - RANGE
    - UNBOUNDED PRECEDING
    - CURRENT ROW
    - FOLLOWING

### ROWS vs RANGE

- ROWS: 물리적 행을 기준으로 범위를 지정
- RANGE: 논리적 값을 기준으로 범위를 지정

### 계층 쿼리

- 데이터가 부모-자식 관계로 구성된 계층적 구조를 조회하기 위한 쿼리
- 트리 구조 데이터를 표현하고 탐색하는데 사용
- START WITH: 계층 구조의 시작점 지정
- CONNECT BY: 부모-자식 관계를 정의
- PRIOR: 계층의 방향성을 설정
- ORDER SIBILINGS BY: 동일 레벨의 데이터 정렬
- LEVEL: 계층 깊이를 나타내는 가상 컬럼
- SYS_CONNECT_BY_PATH: 계층 구조의 경로를 조회

<pre><code>
--1번
SELECT *
FROM(
    SELECT JOB_ID, EMPLOYEE_ID, NAME, SALARY,
    ROW_NUMBER() OVER (PARTITION BY JOB_ID ORDER BY SALARY DESC NULLS LAST) AS RANK
    FROM EMPLOYEES)
WHERE RANK < 4;


--2번**
SELECT YEAR, TOTAL_SUM, SUM(TOTAL_SUM) OVER (ORDER BY YEAR) AS PREFIX_SUM
FROM (
    SELECT EXTRACT(YEAR FROM HIRE_DATE) AS YEAR, SUM(SALARY) AS TOTAL_SUM
    FROM EMPLOYEES
    GROUP BY EXTRACT(YEAR FROM HIRE_DATE));

SELECT TO_CHAR(HIRE_DATE, 'YYYY') AS HIRE_YEAR, 
       SUM(SALARY) AS YEARLY_TOTAL_SALARYY,
       SUM(SUM(SALARY)) OVER(ORDER BY TO_CHAR(HIRE_DATE, 'YYYY') ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS CUMULATIVE_SALARY
  FROM EMPLOYEES
 GROUP BY TO_CHAR(HIRE_DATE, 'YYYY');


--3번**
SELECT EMPLOYEE_ID, NAME, SALARY, 
    RANK() OVER (ORDER BY SALARY DESC) AS SAL_RANK, 
    ROUND(AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID)) AS DEPT_SAL, 
    CASE
        WHEN SALARY > AVG(SALARY) OVER (PARTITION BY DEPARTMENT_ID) THEN '높다'
        ELSE '낮다'
        END AS "비교"
FROM EMPLOYEES
WHERE SALARY IS NOT NULL;


--4번
SELECT * 
FROM (
    SELECT DISTINCT CUSTOMER_ID,
        SUM(AMOUNT) OVER (PARTITION BY CUSTOMER_ID) AS AMT_SUM
    FROM LOANS
    WHERE LOAN_DATE > ADD_MONTHS(SYSDATE, -12)
    ORDER BY AMT_SUM DESC)
WHERE ROWNUM < 6;


--5번
SELECT EMPLOYEE_ID, SALARY,
    ROUND(AVG(SALARY) OVER (ORDER BY EMPLOYEE_ID ROWS 2 PRECEDING), 2) AS AVG_SALARY
FROM EMPLOYEES;

--6번
SELECT C.NAME, A.BALANCE
FROM CUSTOMERS C
JOIN ACCOUNTS A
ON C.CUSTOMER_ID = A.CUSTOMER_ID
WHERE A.BALANCE > = (SELECT AVG(BALANCE) FROM ACCOUNTS)
UNION
SELECT C.NAME, L.AMOUNT
FROM CUSTOMERS C, LOANS L
WHERE C.CUSTOMER_ID = L.CUSTOMER_ID AND L.AMOUNT > = (SELECT AVG(AMOUNT) FROM LOANS);


--7번
SELECT DISTINCT *
FROM(
    SELECT E.JOB_ID, D.DEPARTMENT_NAME, 
        ROUND(AVG(SALARY) OVER (PARTITION BY JOB_ID)) AS JOB_AVG, 
        ROUND(AVG(SALARY) OVER (PARTITION BY E.DEPARTMENT_ID)) AS DEPT_AVG
    FROM EMPLOYEES E, DEPARTMENTS D
    WHERE e.department_id = d.department_id)
WHERE JOB_AVG >= DEPT_AVG;

--8번
SELECT DISTINCT L.BRANCH_ID, COUNT(*) OVER (PARTITION BY BRANCH_ID) AS COUNT,
    ROUND((SELECT DISTINCT COUNT(*) OVER (PARTITION BY BRANCH_ID) FROM LOANS L1 WHERE STATUS = 'APPROVED' AND L1.BRANCH_ID = L.BRANCH_ID)
    /COUNT(*) OVER (PARTITION BY BRANCH_ID),2) AS PCT
FROM LOANS L;


--9번
SELECT NAME, SALARY
FROM(
    SELECT JOB_ID ,NAME, SALARY, RANK() OVER (PARTITION BY JOB_ID ORDER BY SALARY DESC NULLS LAST) AS RANK
    FROM EMPLOYEES)
WHERE RANK = 1
ORDER BY JOB_ID;

--10번
SELECT DISTINCT D.DEPARTMENT_NAME, 
    COUNT(E.MANAGER_ID) OVER (PARTITION BY E.DEPARTMENT_ID) AS "상사있음", 
    COUNT(*) OVER (PARTITION BY E.DEPARTMENT_ID) - COUNT(E.MANAGER_ID) OVER (PARTITION BY E.DEPARTMENT_ID) AS "상사없음"
FROM EMPLOYEES E, DEPARTMENTS D
WHERE e.department_id = D.DEPARTMENT_ID;
</pre></code>
